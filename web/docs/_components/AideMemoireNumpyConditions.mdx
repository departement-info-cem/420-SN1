import { S, N, K, F, B, C } from '@site/docs/_components/PyCode.mdx';

| Opération | Syntaxe | Description |
|---|---|---|
| Importer NumPy | <C><K>import</K> numpy <K>as</K> np</C> | Convention universelle |
| Créer un masque | <C>masque = tab_numpy > <N>0</N></C> | Tableau de booléens selon la condition |
| Filtrer avec un masque | <C>tab_resultat = tab_numpy[masque]</C> | Garde seulement les éléments qui satisfont |
| Filtrer directement | <C>tab_resultat = tab_numpy[tab_numpy > <N>0</N>]</C> | Raccourci : filtre en une seule ligne |
| Filtre **ET** | <C>tab_resultat = tab_numpy[(tab_numpy > <N>0</N>) & (tab_numpy < <N>10</N>)]</C> | Les deux conditions doivent être vraies |
| Filtre **OU** | <C>tab_resultat = tab_numpy[(tab_numpy < <N>0</N>) \| (tab_numpy > <N>10</N>)]</C> | Au moins une condition vraie |
| Remplacer selon condition | <C>tab_resultat = np.<F>where</F>(tab_numpy < <N>0</N>, <N>0</N>, tab_numpy)</C> | Si condition : met <N>0</N>, sinon garde la valeur |
| Au moins un True | <C>resultat = np.<F>any</F>(tab_numpy < <N>0</N>)</C> | `True` si au moins un élément satisfait |
| Tous True | <C>resultat = np.<F>all</F>(tab_numpy > <N>0</N>)</C> | `True` si tous les éléments satisfont |
