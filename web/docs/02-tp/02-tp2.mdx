---
hide_table_of_contents: true
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

import useBaseUrl from '@docusaurus/useBaseUrl';

# TP2 🧮 Analyse de données

:::danger Révisions importantes en cours
Une révision importante du TP est actuellement en cours. \
Veuillez attendre avant de le commencer. Les questions seront modifiées.
:::

:::warning Explications
Une vidéo expliquant en détail le TP et fournissant quelques conseils est disponible en bas de page.
:::

<Row>
    <Column>
        :::info 🧠 Contexte
        Les données sont partout autour de nous. Savoir les analyser et en tirer des conclusions est une compétence essentielle dans de nombreux domaines, en particulier en science.

        Dans ce TP, vous allez choisir un ensemble de données réelles, les analyser à l'aide de Python et produire des visualisations pour illustrer vos découvertes.
        :::
    </Column>
    <Column>
        :::tip 🎯 Objectifs pédagogiques
        À la fin de ce travail, vous serez capable de :

        - Lire et traiter des fichiers CSV avec Pandas
        - Créer des visualisations de données avec Matplotlib
        - Interpréter des données scientifiques réelles
        - Écrire dans un fichier texte un rapport
        :::
    </Column>
</Row>

:::note Créer le projet
Pour faire ce TP, vous devez créer un nouveau projet PyCharm et utiliser un répertoire GitHub dès le début.

- Si votre enseignant utilise GitHub Classroom, utilisez le répertoire créé par votre enseignant.
- Si votre enseignant n'utilise pas GitHub Classroom, créez votre propre répertoire et partagez-lui l'accès en lecture.
:::

:::danger 👀 Plagiat
# ❗🤖 👀 🚫
Si votre travail est suspecté de plagiat (code copié d'un(e) autre étudiant(e), code généré par IA, notions non
abordées en classe, etc.), deux choses peuvent se produire :
        * Le plagiat est prouvé par nos outils : note de 0, automatiquement.
        * Le plagiat est plutôt évident, mais une validation est requise : vous serez convoqué(e) au bureau de votre
enseignant(e). Vous devrez répondre à certaines questions pour prouver que vous comprenez et maîtrisez le code
qui a été utilisé. Si vous ne réussissez pas à répondre à certaines questions, vous aurez la note
de 0 (si vous ne comprenez pas votre propre code, c'est que vous avez plagié, d'une manière ou d'une autre).
:::

:::info 📝 Pour faire ce TP, vous devez :
- choisir un des projets proposés (voir les onglets plus bas).
- suivre le lien donné pour chercher, trouver et télécharger le fichier CSV de données approprié pour le projet choisi.
- écrire un script Python qui lit le fichier CSV, analyse les données et produit :
    - des figures (en format PNG) en utilisant les bibliothèques Pandas et Matplotlib.
    - un fichier texte contenant les réponses à des questions d'analyse.
:::

:::note <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> 📂 Fichiers dans le projet et structure du fichier analyse.py
    Votre projet doit contenir exactement les 2 scripts Python mentionnés ci-dessous ainsi que le fichier `.gitignore` et rien d'autre (ne laissez traîner aucun script inutile).
    ```
    tp2/
    ├── autocorrection.py    # ce fichier vous est fourni et sert à l'autocorrection de votre travail
    ├── analyse.py           # vous devez créer ce script, il va contenir le code de votre TP
    └── .gitignore           # indique les fichiers que Git doit omettre lors de la création d'un commit
    ```
    Téléchargez le fichier <a href={useBaseUrl('/ressources/tp2/autocorrection.py')} download="autocorrection.py">`autocorrection.py`</a> et le fichier <a href={useBaseUrl('/ressources/tp2/.gitignore')} download=".gitignore">`.gitignore`</a> et placez-les dans votre projet. \
    Tous les autres fichiers utilisés (`CSV`) ou produits (`PNG`, `TXT`) par votre programme ne doivent pas être commités dans votre dépôt Git.

    Concernant la structure du fichier `analyse.py`, il doit :
    - Posséder **exactement les 15 fonctions demandées**, pas 1 de plus ou de moins!
    - Avoir **aucun code dans la portée globale**, tout code doit être dans une fonction;
    - **Respecter toutes les directives et contraintes**, elles seront mentionnées au fur et à mesure.
:::

:::warning Le rôle du fichier .gitignore

GitHub est avant tout une plateforme conçue pour héberger des fichiers légers contenant du code source. \
Par principe, certains éléments ne doivent jamais figurer dans un dépôt Git :

- **Les fichiers volumineux** : Ils ralentissent les opérations de clonage et de mise à jour.
- **Les fichiers de configuration d'IDE** : Par exemple, le dossier `.idea` de **PyCharm**, qui est propre à votre environnement de travail local.
- **Les fichiers générés** : Tout ce qui peut être produit automatiquement par l'exécution de votre programme.

En suivant ces principes, **le fichier CSV** que vous devrez télécharger est particulièrement massif et ne devrait pas être sauvegardé sur GitHub. Vous devrez donc retélécharger le fichier CSV et le replacer dans votre dossier de projet à chaque fois que vous changerez de poste dans les laboratoires du Cégep.
Concernant **les fichiers TXT et PNG**, ils sont dans le cadre de ce TP le résultat direct de l'exécution de votre code, il est donc inutile de les versionner :
nous préférons que votre dépôt ne contienne que la "recette" (votre code) et non les produits finis.

**Le fichier `.gitignore` sert à quoi?** Il indique à Git d'ignorer certains fichiers ou dossiers en fonction de modèles (patterns) définis à l'intérieur. \
Dans le cadre de ce projet, il a été configuré pour exclure automatiquement les fichiers `CSV`, `PNG` et `TXT` de vos commits.

:::

:::info <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> Répertoire Git 🐈‍⬛ et documentation des fonctions
- Au moins 5 commits de tailles comparables (il n'y a pas un commit avec tout dedans et les autres vides)
- Les commits décrivent l'avancement du projet dans un français sans faute [(voir instructions)](https://info.cegepmontpetit.ca/git)
- Chaque fonction doit être documentée à l'aide d'un  [docstring](/cours/rencontre3?onglet=comments)  dans un français sans faute.
:::

:::danger ⚠️ Note importante sur l'utilisation d'Excel ⚠️
Bien qu'Excel soit l'outil par défaut pour lire des fichiers CSV, il n'est pas adapté aux jeux de données massifs scientifique que nous utilisons.

- **Affichage incomplet** : Au-delà d'un certain seuil (environ 1 million de lignes), Excel tronque les données et n'affiche pas la totalité du fichier.
- **Risque d'altération** : Excel a tendance à reformater automatiquement les données, ce qui peut corrompre l'intégrité de votre fichier CSV.

**Conseil** : Évitez d'ouvrir le fichier CSV avec Excel!
:::

:::tip
- Commencez par explorer les données pour comprendre leur structure et leur contenu, en utilisant des fonctions comme `head()`, `info()` et `describe()` de Pandas.
- Nettoyez les données si nécessaire (gestion des valeurs manquantes, conversion des types de données, etc.).
- Utilisez des fonctions Pandas pour effectuer des analyses statistiques de base (moyennes, médianes, écarts-types, etc.).
- Créez des visualisations claires et informatives avec Matplotlib, en utilisant des graphiques adaptés aux types de données analysées (graphique linéaire, diagramme à barres, histogramme, etc.).
:::

### Choisissez un des projets suivants :

<Tabs>

    <TabItem value="velos" label="📌 Projet vélos 🚲" default>

        ## Projet : 🚲 Comptages des vélos sur les pistes cyclables de Montréal

        :::info 📝 Description
        - La Ville de Montréal a installé des compteurs de vélos sur plusieurs pistes cyclables pour suivre l'utilisation du réseau cyclable. L'objectif de ce projet est d'analyser les données de comptage des vélos pour identifier les tendances saisonnières, hebdomadaires et journalières dans l'utilisation des pistes cyclables.
        :::

        :::note 🔢 Source de données
        - Données Québec : [Comptages des vélos sur les pistes cyclables de Montréal](https://www.donneesquebec.ca/recherche/dataset/vmtl-velos-comptage)
        - Téléchargez le fichier <i>Vélos - comptage sur les pistes cyclables, 2024</i>.
        - On veut analyser les données de l'année 2024 (car celles de 2025 ne sont pas encore complètes).
        :::

        :::info <Highlight color="tip"><span style={{color: 'white'}}>3 points</span></Highlight> 🔍 Éléments à analyser
        Écrivez du code pour répondre aux questions suivantes (écrivez une fonction par question) :
        - **#1** - Quelles sont les variables présentes dans le jeu de données? \
          *La fonction doit retourner une `liste de string` et être la seule à posséder dans sa docstring l'abréviation `QA1`*.

        - **#2** - Combien y a-t-il d'enregistrements (lignes) dans le jeu de données?\
          *La fonction doit retourner un `int` et être la seule à posséder dans sa docstring l'abréviation `QA2`*.

        - **#3** - Quel est le plus grand nombre de vélos comptés en une seule fois? À quel moment cela s'est-il produit (date, heure, emplacement)?\
          *La fonction doit retourner un `tuple` avec les 4 éléments (nb, date, heure, emplacement) et être la seule à posséder dans sa docstring l'abréviation `QA3`*.

        - **#4** - Quel est le total de vélos comptés pour l'année 2024?\
          *La fonction doit retourner un `int` et être la seule à posséder dans sa docstring l'abréviation `QA4`*.

        - **#5** - Combien y a-t-il de compteurs de vélos différents?\
          *La fonction doit retourner un `int` et être la seule à posséder dans sa docstring l'abréviation `QA5`*.

        - **#6** - TODO\
          *TODO*

        **CONTRAINTES À RESPECTER** \
        Concernant ces fonctions, elles doivent :
        - Pouvoir être appelées sans paramètre;
        - Retourner le résultat dans le format indiqué;
        - Chacune faire appel à la fonction `pd.read_csv(...)`;
        - Chacune nettoyer, si nécessaire, les données en fonction des besoins de la question;
        - Posséder l'abréviation demandée dans les docstrings (nécessaire pour l'autocorrecteur).

        Aucune de ces fonctions doivent appeller à la fonction `print(...)`!
        :::

        :::info <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> ✏️ Création du fichier sommaire.txt
        Développez une fonction dont le rôle est d'enregistrer toutes les questions d'analyse et leurs réponses respectives dans un seul fichier texte.

        - **Nom du fichier** : Le fichier doit impérativement s'appeler `sommaire.txt`.
        - **Contenu** : Il doit inclure à la fois l'énoncé de la question (ex : « Quel est... ? ») et la réponse obtenue (ex : « 52 »).
        - **Documentation** : La docstring de la fonction doit obligatoirement inclure l'abréviation `QASommaire`.
        - **Structure** : Cette fonction doit appeler les six fonctions développées précédemment pour récupérer les données nécessaires.

        Cette fonction ne prend aucun paramètre et ne retourne aucune valeur.
        :::

        :::::info <Highlight color="tip"><span style={{color: 'white'}}>4 points</span></Highlight> 📊 Figures à produire
        Vous devez produire **4 figures**, chacune nécessitant la création de deux fonctions distinctes (pour un **total de 8 fonctions**).

        **1. Fonction de préparation des données (Pandas)** \
        Son rôle est d'extraire et de formater les données. Elle doit :
        - S’exécuter sans paramètre;
        - Utiliser `pd.read_csv(...)` pour charger les données;
        - Effectuer le nettoyage de données requis par la problématique lorsque vous le jugez nécessaire;
        - Retourner un DataFrame contenant exclusivement les colonnes et les lignes nécessaires à la figure;
        - Mentionner l'abréviation qui sera demandée dans sa docstring.

        Un aperçu du DataFrame est mis à votre disposition pour faciliter votre autovalidation. \
        Assurez-vous que la première fonction retourne le résultat attendu avant de débuter la seconde.

        **2. Fonction de visualisation (Matplotlib)** \
        Son rôle est de produire l'image finale. Elle doit :
        - Rien retourner.
        - S'exécuter sans paramètre;
        - Appeler la fonction de préparation correspondante pour obtenir les données;
        - Générer le graphique demandé à l'aide de matplotlib (sur le DataFrame);
        - Exporter le résultat final au format `.png`;
        - Mentionner l'abréviation qui sera demandée dans sa docstring.

        ::::note <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> Figure 1
        <Row>
            <Column>
                <img src={require('/img/tp2/p1/figure1.png').default}/>
            </Column>
            <Column>
                <br/>
                <br/>
                <br/>
                <img src={require('/img/tp2/p1/dataframe1.png').default}/>
            </Column>
        </Row>
        - La docstring de la première fonction doit avoir l'abréviation `F1Data` et la seconde `F1Graphique`;
        - L'image générée doit se nommée `figure1.png`.

        :::tip Astuces
        - Créer une nouvelle colonne pour les 2 chiffres de l’heure (ex : hh) sans les minutes et secondes;
        - Grouper les données par les 2 chiffres de l’heure et calculer la moyenne du nombre de passages;
        - Vous pouvez utiliser `df.plot(kind="bar")`.
        :::
        ::::

        ::::note <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> Figure 2
        <Row>
            <Column>
                <img src={require('/img/tp2/p1/figure2.png').default}/>
            </Column>
            <Column>
                <br/>
                <br/>
                <br/>
                <img src={require('/img/tp2/p1/dataframe2.png').default}/>
            </Column>
        </Row>
        - La docstring de la première fonction doit avoir l'abréviation `F2Data` et la seconde `F2Graphique`;
        - L'image générée doit se nommée `figure2.png`.

        :::tip Astuces
        - Créez un dictionnaire ayant comme clé le numéro du mois et comme valeur son nom.
        - Utilisez la méthode `map` pour créer une nouvelle colonne pour le nom du mois.
        :::
        ::::

        ::::note <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> Figure 3
        <Row style={{ display: 'flex', alignItems: 'center' }} className="align-items--center">
            <Column>
                <img src={require('/img/tp2/p1/figure3.png').default}/>
            </Column>
            <Column>
                <br/>
                <br/>
                <br/>
                <img src={require('/img/tp2/p1/dataframe3.png').default}/>
            </Column>
        </Row>
        - La docstring de la première fonction doit avoir l'abréviation `F3Data` et la seconde `F3Graphique`;
        - L'image générée doit se nommée `figure3.png`.

        :::tip Astuces
        - Utilisez un tableau croisé dynamique (pivot table).
        :::
        ::::

        ::::note <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> Figure 4
        <Row>
            <Column>
                <img src={require('/img/tp2/p1/figure4.png').default}/>
            </Column>
            <Column>
                <br/>
                <br/>
                <br/>
                <img src={require('/img/tp2/p1/dataframe4.png').default}/>
            </Column>
        </Row>
        - La docstring de la première fonction doit avoir l'abréviation `F4Data` et la seconde `F4Graphique`;
        - L'image générée doit se nommée `figure4.png`.

        :::tip Astuces
        - Regroupez les données par compteur, puis calculez la moyenne.
        - La taille des points est le nombre moyen de passages x 10.
        - La transparence est de 50%.
        :::
        ::::

        :::::

    </TabItem>

    <TabItem value="p2" label="📌 Projet arbres 🌳">

        ## Projet : 🌳 Analyse des arbres répertoriés dans la Ville de Québec

        :::info 📝 Description
        - La Ville de Québec a répertorié les arbres situés sur son territoire. L'objectif de ce projet est d'analyser ces données pour comprendre la répartition des espèces d'arbres, leur taille, etc.
        :::

        :::note 🔢 Source de données
        - Données Québec : [Arbres répertoriés dans la Ville de Québec](https://www.donneesquebec.ca/recherche/dataset/vque_26)
        - Téléchargez le fichier CSV.
        :::

        :::info <Highlight color="tip"><span style={{color: 'white'}}>3 points</span></Highlight> 🔍 Éléments à analyser
        Écrivez du code pour répondre aux questions suivantes (écrivez une fonction par question) :
        - **#1** - Quelles sont les variables présentes dans le jeu de données? \
          *La fonction doit retourner une `liste de string` et être la seule à posséder dans sa docstring l'abréviation `QA1`*.

        - **#2** - Combien y a-t-il d'enregistrements (lignes) dans le jeu de données?\
          *La fonction doit retourner un `int` et être la seule à posséder dans sa docstring l'abréviation `QA2`*.

        - **#3** - Combien y a-t-il d'espèces d'arbres différentes?\
          *La fonction doit retourner un `tuple` avec les 4 éléments (nb, date, heure, emplacement) et être la seule à posséder dans sa docstring l'abréviation `QA3`*.

        - **#4** - Quels noms d'espèces d'arbres (en français) font moins de 20 caractères?\
          *La fonction doit retourner un `int` et être la seule à posséder dans sa docstring l'abréviation `QA4`*.

        - **#5** - De quelle espèce est l'arbre avec le tronc le plus gros?\
          *La fonction doit retourner un `int` et être la seule à posséder dans sa docstring l'abréviation `QA5`*.

        - **#6** - Quel est le diamètre moyen du tronc des feuillus dans la ville de Québec? Des conifères?\
           *La fonction doit retourner un `dictionnaire` avec les 2 entrées ("diametre_feuillus" et "diametre_coniferes") et être la seule à posséder dans sa docstring l'abréviation `QA6`*.

        **CONTRAINTES À RESPECTER** \
        Concernant ces fonctions, elles doivent :
        - Pouvoir être appelées sans paramètre;
        - Retourner le résultat dans le format indiqué;
        - Chacune faire appel à la fonction `pd.read_csv(...)`;
        - Chacune nettoyer, si nécessaire, les données en fonction des besoins de la question;
        - Posséder l'abréviation demandée dans les docstrings (nécessaire pour l'autocorrecteur).

        Aucune de ces fonctions doivent appeller à la fonction `print(...)`!
        :::

        :::info <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> ✏️ Création du fichier sommaire.txt
        Développez une fonction dont le rôle est d'enregistrer toutes les questions d'analyse et leurs réponses respectives dans un seul fichier texte.

        - **Nom du fichier** : Le fichier doit impérativement s'appeler `sommaire.txt`.
        - **Contenu** : Il doit inclure à la fois l'énoncé de la question (ex : « Quel est... ? ») et la réponse obtenue (ex : « 52 »).
        - **Documentation** : La docstring de la fonction doit obligatoirement inclure l'abréviation `QASommaire`.
        - **Structure** : Cette fonction doit appeler les six fonctions développées précédemment pour récupérer les données nécessaires.

        Cette fonction ne prend aucun paramètre et ne retourne aucune valeur.
        :::

        :::::info <Highlight color="tip"><span style={{color: 'white'}}>4 points</span></Highlight> 📊 Figures à produire
        Vous devez produire **4 figures**, chacune nécessitant la création de deux fonctions distinctes (pour un **total de 8 fonctions**).

        **1. Fonction de préparation des données (Pandas)** \
        Son rôle est d'extraire et de formater les données. Elle doit :
        - S’exécuter sans paramètre;
        - Utiliser `pd.read_csv(...)` pour charger les données;
        - Effectuer le nettoyage de données requis par la problématique lorsque vous le jugez nécessaire;
        - Retourner un DataFrame contenant exclusivement les colonnes et les lignes nécessaires à la figure;
        - Mentionner l'abréviation qui sera demandée dans sa docstring.

        Un aperçu du DataFrame est mis à votre disposition pour faciliter votre autovalidation. \
        Assurez-vous que la première fonction retourne le résultat attendu avant de débuter la seconde.

        **2. Fonction de visualisation (Matplotlib)** \
        Son rôle est de produire l'image finale. Elle doit :
        - Rien retourner.
        - S'exécuter sans paramètre;
        - Appeler la fonction de préparation correspondante pour obtenir les données;
        - Générer le graphique demandé à l'aide de matplotlib (sur le DataFrame);
        - Exporter le résultat final au format `.png`;
        - Mentionner l'abréviation qui sera demandée dans sa docstring.

        ::::note <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> Figure 1
        <Row>
            <Column>
                <img src={require('/img/tp2/p2/figure1.png').default}/>
            </Column>
            <Column>
                <br/>
                <br/>
                <br/>
                <img src={require('/img/tp2/p2/dataframe1.png').default}/>
            </Column>
        </Row>
        - La docstring de la première fonction doit avoir l'abréviation `P2F1Data` et la seconde `P2F1Graphique`;
        - L'image générée doit se nommée `figure1.png`.

        :::tip Astuces
        - Pour mettre les étiquettes de l'axe des x à 45$^\circ$, utilisez l'instruction `plt.xticks(rotation=45, ha='right')`
        - Regroupez vos données et générez des statistiques avant la visualisation.
        - Vous pouvez utiliser `df.plot(kind="bar")`.
        :::
        ::::

        ::::note <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> Figure 2
        <Row>
            <Column>
                <img src={require('/img/tp2/p2/figure2.png').default}/>
            </Column>
            <Column>
                <br/>
                <br/>
                <br/>
                <img src={require('/img/tp2/p2/dataframe2.png').default}/>
            </Column>
        </Row>
        - La docstring de la première fonction doit avoir l'abréviation `P2F2Data` et la seconde `P2F2Graphique`;
        - L'image générée doit se nommée `figure2.png`.

        :::tip Astuces
        - Créez un dictionnaire ayant comme clé le numéro du mois et comme valeur son nom.
        - Utilisez la méthode `map` pour créer une nouvelle colonne pour le nom du mois.
        :::
        ::::

        ::::note <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> Figure 3
        <Row style={{ display: 'flex', alignItems: 'center' }} className="align-items--center">
            <Column>
                <img src={require('/img/tp2/p2/figure3.png').default}/>
            </Column>
            <Column>
                <br/>
                <br/>
                <br/>
                <img src={require('/img/tp2/p2/dataframe3.png').default}/>
            </Column>
        </Row>
        - La docstring de la première fonction doit avoir l'abréviation `P2F3Data` et la seconde `P2F3Graphique`;
        - L'image générée doit se nommée `figure3.png`.

        :::tip Astuces
        - Récupérez une liste des types d'arbres.
        - Pour chacun des types d'arbres de la liste, récupérez les données puis ajoutez-les au graphique à l'aide d'un nuage de points.
        - Lors de la création des nuages de points, ajoutez le paramètre `s=0.01`.
        - **Pour l'affichage de la légende, utilisez l'instruction `plt.legend(title="Type d'arbre", markerscale=50)`**
        :::
        ::::

        ::::note <Highlight color="tip"><span style={{color: 'white'}}>1 point</span></Highlight> Figure 4
        <Row>
            <Column>
                <img src={require('/img/tp2/p2/figure4.png').default}/>
            </Column>
            <Column>
                <br/>
                <br/>
                <br/>
                <img src={require('/img/tp2/p2/dataframe4.png').default}/>
            </Column>
        </Row>
        - La docstring de la première fonction doit avoir l'abréviation `P2F4Data` et la seconde `P2F4Graphique`;
        - L'image générée doit se nommée `figure4.png`.

        :::tip Astuces
        - Récupérez une liste des espèces pour la région spécifiée.
        - Pour chacun des espèces de la liste, récupérez les données puis ajoutez-les au graphique à l'aide d'un nuage de points.
        - **Pour l'affichage de la légende, utilisez l'instruction `plt.legend(title="Type d'arbre", loc='lower left', prop={'size': 6}, title_fontsize=7, markerscale=0.6)`**
        :::
        ::::
        :::::

    </TabItem>

</Tabs>


:::tip ✅ Autocorrection

Vous devez exécuter fréquemment le script `autocorrection.py`. Il détecte automatiquement votre progression, ce que vous avez fait ou non, et exécute des tests en conséquence.
Votre enseignant va se servir de ce script pour valider votre TP2.

⚠️ Pour que le script fonctionne correctement, il faut absolument que les fonctions possèdent dans leurs docstrings les abréviations demandées.

Le script nécessite au préalable l'installation du package `requests` ([voir la procédure d'installation d'un package](/recettes/ajouter-librairie)).
Cette bibliothèque permet de télécharger des fichiers en ligne par programmation ; elle est utilisée ici pour vérifier si une nouvelle version du script est disponible et pour la télécharger automatiquement.


**Limitations du script** \
Le script possède certaines limites et ne vérifie pas les points suivants :

- La **cohérence de vos docstrings** ainsi que la **qualité du français**.
- La présence de **code inutile ou redondant** à l'intérieur de vos fonctions.
- Le respect de la consigne exigeant au moins **cinq commits** dans votre dépôt GitHub.

:::

:::danger Fonctionnement global <Highlight color="danger"><span style={{color: 'white'}}>2 points négatifs</span></Highlight>
Le projet fonctionne sans plantage et correctement, et le code est clair et facile à lire.
Ce pointage fonctionne en négatif.
Si le projet fonctionne correctement en tout temps, vous conservez votre note.
Dans le cas contraire, vous perdez des points avec un maximum de 2.

- Plantage (un script qui plante lorsqu'on l'exécute) <Highlight color="danger"><span style={{color: 'white'}}>-1 point</span></Highlight>
- Code illisible (ex : noms de variables incompréhensibles, difficulté à comprendre le code) <Highlight color="danger"><span style={{color: 'white'}}>-1 point</span></Highlight>
- Information affichée incohérente <Highlight color="danger"><span style={{color: 'white'}}>-1 point</span></Highlight>
- Autre cas...
:::

## Explications détaillées
<iframe
    style={{
        width: "100%",
        aspectRatio: "16 / 9",
        border: "0",
    }}
    src="https://www.youtube.com/embed/F_M8CuuXGTE?si=V8eLwSB5FQEvupuZ"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
    allowFullScreen
></iframe>