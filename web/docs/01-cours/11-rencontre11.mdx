---
title: 11 - 🐼 Pandas
description: Analyse de données, Pandas
hide_table_of_contents: true
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import useBaseUrl from '@docusaurus/useBaseUrl';

import RappelFonctionnementClasseInverse from '@site/docs/_components/RappelFonctionnementClasseInverse.mdx';
import Feedback from "../../src/components/Feedback";
import { S, N, K, F, B, C } from '@site/docs/_components/PyCode.mdx';
import AideMemoirePandasExplorer from '@site/docs/_components/AideMemoirePandasExplorer.mdx';
import AideMemoirePandasFiltrerTrier from '@site/docs/_components/AideMemoirePandasFiltrerTrier.mdx';
import AideMemoirePandasSelectionner from '@site/docs/_components/AideMemoirePandasSelectionner.mdx';
import AideMemoirePandasParcourir from '@site/docs/_components/AideMemoirePandasParcourir.mdx';
import AideMemoirePandasNettoyer from '@site/docs/_components/AideMemoirePandasNettoyer.mdx';
import AideMemoirePandasTransformer from '@site/docs/_components/AideMemoirePandasTransformer.mdx';
import AideMemoirePandasStatistiques from '@site/docs/_components/AideMemoirePandasStatistiques.mdx';
import AideMemoirePandasExporter from '@site/docs/_components/AideMemoirePandasExporter.mdx';
import AideMemoirePandasVisualiser from '@site/docs/_components/AideMemoirePandasVisualiser.mdx';

# 🐼 Pandas

<Tabs queryString="onglet">

    <TabItem value="data" label="Intro" default>

        :::tip 💡 Cette rencontre en bref

        Jusqu’ici, tu as appris à écrire des programmes Python : variables, boucles, fonctions, listes, dictionnaires, fichiers. \
        Ces outils sont puissants, mais quand on doit analyser des **centaines ou des milliers de données**, les manipuler une par une devient vite impraticable.

        C’est là qu’intervient 🐼 **Pandas** : une bibliothèque Python conçue pour charger, explorer, transformer et analyser des données tabulaires (comme des fichiers Excel ou CSV) en quelques lignes de code.

        :::

        ---

        :::info 📋 Ce que tu vas apprendre dans cette rencontre

        Pour ton **TP2**, tu devras manipuler des **données réelles**. Voici les étapes typiques d’un pipeline d’analyse, et les onglets qui y correspondent :

        | Étape | Ce qu’on fait | Onglet |
        |---|---|---|
        | 📥 Charger | Lire un fichier CSV dans un DataFrame | **Explorer** |
        | 🔍 Filtrer | Filtrer et trier les données | **Filtrer / Trier** |
        | 🎯 Sélectionner | Choisir des colonnes ou des lignes | **Sélectionner** |
        | 🔁 Parcourir | Itérer sur les lignes ou colonnes | **Parcourir** |
        | 🧹 Nettoyer | Gérer les valeurs manquantes et les doublons | **Nettoyer** |
        | 🧮 Transformer | Créer de nouvelles colonnes | **Transformer** |
        | 📊 Analyser | Calculer des statistiques, regrouper | **Statistiques** |
        | 📈 Visualiser | Tracer des graphiques | **Visualiser** |

        :::

        ---

        :::note 🧰 Le contexte plus large

        Pandas n’est qu’une des bibliothèques utilisées en science des données. Pour ta culture générale, voici l’écosystème Python dans ce domaine :

        - 🐼 **Pandas** : manipulation de données tabulaires — **c’est le sujet de cette rencontre**
        - 🎨 **Matplotlib** : visualisation de données — **tu le connais déjà**
        - 🔢 **NumPy** : calcul scientifique sur des tableaux — **vu à la rencontre 10**
        - 🤖 **Scikit-learn** : apprentissage automatique — **prochain cours**
        - 🧠 **Keras / TensorFlow** : apprentissage profond (*deep learning*) — **cours ultérieurs**

        :::

        <Feedback feedbackId="Rencontre #11 - Intro" />
    </TabItem>

    <TabItem value="pandas" label="Explorer">

        :::tip 💡 Le DataFrame : un dictionnaire de listes

        Tu connais déjà les **dictionnaires** et les **listes** en Python. Un *DataFrame* Pandas, c'est exactement ça : un **dictionnaire dont les valeurs sont des listes de même longueur**.

        ```python
        import pandas as pd

        data = {
            "Nom":   ["Alice", "Bob", "Charlie"],
            "Âge":   [25,      30,    35],
            "Ville": ["Paris", "Lyon", "Marseille"]
        }
        df = pd.DataFrame(data)
        print(df)
        ```

        ```
               Nom  Âge      Ville
        0    Alice   25      Paris
        1      Bob   30       Lyon
        2  Charlie   35  Marseille
        ```

        Quelques points importants à observer :
        - Les **clés** du dictionnaire deviennent les **noms des colonnes**.
        - Chaque **liste** devient une **colonne** de données.
        - La colonne `0, 1, 2` à gauche est l'**index** (numéro de ligne automatique).
        - Chaque ligne est un **enregistrement** (ici, une personne).

        Un *DataFrame* peut avoir des **milliers de lignes** : c'est là que Pandas devient indispensable, là où manipuler une liste de dictionnaires ordinaire deviendrait vite impraticable.

        :::

        ---

        :::info 📥 Charger un fichier CSV dans un DataFrame

        Dans l'exemple précédent, les données étaient écrites directement dans le code. En pratique, les données proviennent presque toujours de **fichiers externes** : bases de données, exports Excel, JSON… Le format le plus courant est le **CSV** (*Comma-Separated Values*) : un simple fichier texte où chaque ligne est un enregistrement et chaque valeur est séparée un délimiteur (souvent une virgule).

        Pandas permet de charger un fichier CSV en une seule ligne avec `pd.read_csv()`, qui retourne directement un **DataFrame** prêt à être exploré.

        Le fichier <a href={useBaseUrl('/ressources/csv/tips_dataset.csv')} download="tips_dataset.csv">tips_dataset.csv</a> regroupe des données collectées dans un restaurant : pour chaque repas, on y retrouve le montant de la facture, le pourboire laissé, le jour, le moment de la journée, la taille du groupe, et quelques informations sur le serveur.

        Télécharge-le, place-le à la racine de ton projet PyCharm, puis charge-le :

        ```python
        import pandas as pd

        df = pd.read_csv('tips_dataset.csv')  # → retourne un DataFrame
        ```

        Ces données serviront à illustrer toutes les opérations qui suivent! 🎯

        :::

        ---

        :::info 👁️ df.head() — aperçu des premières lignes

        `df.head()` retourne les 5 premières lignes par défaut. Tu peux passer un nombre en argument pour en voir plus.

        ```python
        print(df.head())    # → les 5 premières lignes
        print(df.head(20))  # → les 20 premières lignes
        ```

        Résultat :
        ```
           total_bill   tip     sex smoker  day    time  size
        0       16.99  1.01  Female     No  Sun  Dinner     2
        1       10.34  1.66    Male     No  Sun  Dinner     3
        2       21.01  3.50    Male     No  Sun  Dinner     3
        3       23.68  3.31    Male     No  Sun  Dinner     2
        4       24.59  3.61  Female     No  Sun  Dinner     4
        ```

        :::

        ---

        :::info 📐 df.shape — dimensions du DataFrame

        `df.shape` retourne un tuple `(nombre de lignes, nombre de colonnes)`. \
        C'est la façon la plus rapide de connaître la taille d'un DataFrame.

        ```python
        print(df.shape)        # → (244, 7)
        print(df.shape[0])     # → 244  (nombre de lignes)
        print(df.shape[1])     # → 7    (nombre de colonnes)
        ```

        :::

        ---

        :::info 🏷️ df.columns — noms des colonnes

        `df.columns` retourne les noms des colonnes. On appelle `.tolist()` pour obtenir une liste Python ordinaire.

        ```python
        print(df.columns.tolist()) # → ['total_bill', 'tip', 'sex', 'smoker', 'day', 'time', 'size']
        ```

        :::

        ---

        :::note 🏷️ Les types (Dtype) dans Pandas

        Dans un DataFrame, chaque colonne a un type de données (`Dtype`). Voici les types les plus courants et leur équivalent Python :

        | Dtype Pandas | Équivalent Python | Description |
        |---|---|---|
        | <C>int64</C> | <C><B>int</B></C> | Nombre entier |
        | <C>float64</C> | <C><B>float</B></C> | Nombre décimal |
        | <C>object</C> | <C><B>str</B></C> | Texte (chaîne de caractères) |
        | <C>bool</C> | <C><B>bool</B></C> | Valeur booléenne (<C><K>True</K></C> / <C><K>False</K></C>) |

        :::

        ---

        :::info 🔎 df.info() — types et valeurs manquantes

        `df.info()` affiche le nombre de lignes, le nom et le `Dtype` de chaque colonne, et le nombre de valeurs non-nulles. \
        C'est souvent la première chose à appeler pour détecter des colonnes mal typées ou des valeurs manquantes.

        ```python
        df.info()   # affiche directement — pas besoin de print()
        ```

        Résultat :
        ```
        <class 'pandas.core.frame.DataFrame'>
        RangeIndex: 244 entries, 0 to 243
        Data columns (total 7 columns):
         #   Column      Non-Null Count  Dtype
        ---  ------      --------------  -----
         0   total_bill  244 non-null    float64
         1   tip         244 non-null    float64
         2   sex         244 non-null    object
         3   smoker      244 non-null    object
         4   day         244 non-null    object
         5   time        244 non-null    object
         6   size        244 non-null    int64
        dtypes: float64(2), int64(1), object(3)
        memory usage: 13.5+ KB
        ```

        - <C>RangeIndex: 244 entries</C> — 244 lignes, indexées de 0 à 243
        - <C>float64</C> — nombre décimal, aucune valeur manquante (<C>244 non-null</C>)
        - <C>object</C> — texte (<C>str</C> en Python)
        - <C>int64</C> — nombre entier
        - <C>dtypes: float64(2), int64(1), object(3)</C> — résumé des types présents dans le DataFrame

        :::

        ---

        ::::info 📊 df.describe() — statistiques descriptives

        `df.describe()` retourne un résumé statistique rapide pour toutes les colonnes numériques.

        ```python
        print(df.describe())
        ```

        Résultat :
        ```
               total_bill         tip        size
        count  244.000000  244.000000  244.000000
        mean    19.785943    2.998279    2.569672
        std      8.902412    1.383638    0.951100
        min      3.070000    1.000000    1.000000
        25%     13.347500    2.000000    2.000000
        50%     17.795000    2.900000    2.000000
        75%     24.127500    3.562500    3.000000
        max     50.810000   10.000000    6.000000
        ```

        - <C>count</C> — nombre de valeurs non-nulles
        - <C>mean</C> — moyenne
        - <C>std</C> — écart-type (mesure la dispersion des valeurs)
        - <C>min</C> / <C>max</C> — valeur minimale et maximale
        - <C>25%</C> / <C>50%</C> / <C>75%</C> — quartiles (50% = médiane)

        ::::

        ---

        :::note 💡 Astuce PyCharm : explorer un CSV visuellement

        Avant d'écrire du code, tu peux explorer ton fichier CSV directement dans PyCharm en faisant un **double-clic** dessus. Deux modes sont disponibles en bas de l'éditeur : **Mode Text** (contenu brut) et **Mode Data** (tableau interactif avec filtre, tri et recherche).

        <Row>
          <Column size={6}>
            <div style={{ textAlign: "center" }}>
              <img
                src={require('/img/cours/R11-lecturecsv-mode-text.png').default}
                alt="Mode Text dans PyCharm"
                style={{ maxWidth: '100%', height: 'auto' }}
              />
              <p><strong>Mode Text</strong> - affichage brut du fichier</p>
            </div>
          </Column>
          <Column size={6}>
            <div style={{ textAlign: "center" }}>
              <img
                src={require('/img/cours/R11-lecturecsv-mode-data.png').default}
                alt="Mode Data dans PyCharm"
                style={{ maxWidth: '100%', height: 'auto' }}
              />
              <p><strong>Mode Data</strong> - affichage tabulaire interactif</p>
            </div>
          </Column>
        </Row>

        <br />

        ⚠️ PyCharm (comme Excel, limité à ~1 million de lignes) n'affiche pas nécessairement toutes les lignes pour des raisons de performance. C'est précisément pourquoi Pandas est indispensable : il peut traiter des fichiers de plusieurs millions de lignes sans limitation.

        :::

        ---

        :::warning Ajuster l'affichage des *DataFrames* dans la console
        Si ton *DataFrame* a trop de colonnes ou du contenu trop long, certaines colonnes peuvent être remplacées par des `...`. \
        Dans ce cas, ajoute ces 2 instructions juste après tes importations en haut de ton script :

        ```python showLineNumbers
        import pandas as pd
        pd.set_option('display.max_colwidth', None)
        pd.set_option('display.expand_frame_repr', False)
        ```
        :::

        ---

        ::::note 📋 Aide-mémoire — Explorer un DataFrame

        <AideMemoirePandasExplorer />

        ::::

        <Feedback feedbackId="Rencontre #11 - DataFrame" />
    </TabItem>


    <TabItem value="pandasfiltrer" label="Filtrer / Trier">

        :::info 🔍 Qu'est-ce qu'un filtre ?

        Filtrer un DataFrame, c'est **ne garder que les lignes qui satisfont une condition**, comme un crible qui laisse passer certaines données et en bloque d'autres.

        Par exemple, sur un DataFrame de 244 repas, on pourrait vouloir garder seulement :
        - les repas où le pourboire dépasse 5 $
        - les repas du dimanche soir
        - les repas avec un groupe de plus de 4 personnes

        Le résultat est toujours un **nouveau DataFrame**. Le DataFrame original `df` n'est jamais modifié.

        :::

        ---

        :::info 🔬 Comment Pandas applique un filtre

        Quand on écrit `df[df["tip"] > 5]`, Pandas effectue deux étapes :

        1. `df["tip"] > 5` produit une **série de booléens** : `True` pour chaque ligne où le pourboire dépasse 5, `False` sinon
        2. `df[...]` ne conserve que les lignes où la valeur est `True`

        ```python
        df_gros_pourboires = df[df["tip"] > 5]
        print(df_gros_pourboires)
        ```

        Résultat :
        ```
            total_bill   tip     sex smoker   day    time  size
        23       39.42  7.58    Male     No   Sat  Dinner     4
        44       30.40  5.60    Male     No   Sun  Dinner     4
        47       32.68  5.00    Male     No   Sat  Dinner     3
        52       34.81  5.20  Female     No   Sun  Dinner     4
        59       48.27  6.73    Male     No   Sat  Dinner     4
        ...
        ```

        :::

        ---

        :::info 🔢 Filtre simple

        Un filtre simple compare les valeurs d'une colonne à une valeur de référence. On peut utiliser les opérateurs de comparaison habituels.

        ```python
        # Comparaison numérique
        df_gros_pourboires  = df[df["tip"] > 5]              # pourboire supérieur à 5 $
        df_petites_tables   = df[df["size"] <= 2]            # tables de 1 ou 2 personnes
        df_facture_exacte   = df[df["total_bill"] == 16.99]  # montant exact

        # Comparaison sur du texte (égalité exacte)
        df_souper           = df[df["time"] == "Dinner"]     # repas du soir seulement
        df_non_fumeurs      = df[df["smoker"] == "No"]       # tables non-fumeurs
        df_samedi           = df[df["day"] == "Sat"]         # repas du samedi
        ```

        :::

        ---

        :::info 🔤 Filtre texte

        Pour filtrer sur le contenu d'une colonne textuelle, Pandas offre des méthodes `.str.`. \
        Elles sont utiles quand on veut chercher une sous-chaîne plutôt que vérifier une égalité exacte.

        ```python
        df_jours_th    = df[df["day"].str.contains("Th")]   # "Thur" contient "Th"
        df_jours_longs = df[df["day"].str.len() > 3]        # "Thur" a 4 caractères, "Fri" en a 3
        df_majuscule_f = df[df["sex"].str.startswith("F")]  # "Female" commence par "F"
        ```

        :::

        ---

        ::::info 🔗 Filtre ET / OU

        On peut combiner plusieurs conditions avec `&` (ET) et `|` (OU). Chaque condition doit être entre parenthèses.

        ```python
        # & : les deux conditions doivent être vraies
        df_dimanche_souper     = df[(df["day"] == "Sun") & (df["time"] == "Dinner")]
        df_fumeurs_genereux    = df[(df["smoker"] == "Yes") & (df["tip"] > 4)]

        # | : au moins une des conditions doit être vraie
        df_extremes_facture    = df[(df["total_bill"] < 5) | (df["total_bill"] > 45)]
        df_repas_courts        = df[(df["time"] == "Lunch") | (df["size"] == 1)]
        ```

        :::warning ⚠️ Parenthèses obligatoires
        Sans parenthèses autour de chaque condition, Python interprète mal l'expression et génère une erreur. Écris toujours `(cond1) & (cond2)`, jamais `cond1 & cond2`.
        :::

        ::::

        ---

        ::::info 📋 Filtre liste

        Quand on veut filtrer sur plusieurs valeurs d'une même colonne, `isin()` est plus lisible qu'une suite de `|`.

        ```python
        # Garder seulement les repas du jeudi et du vendredi
        df_jours_semaine = df[df["day"].isin(["Thur", "Fri"])]

        # Garder seulement les grandes tables (4, 5 ou 6 personnes)
        df_grandes_tables = df[df["size"].isin([4, 5, 6])]
        ```

        :::note 💡 `isin()` vs `|`
        `df[df["day"].isin(["Thur", "Fri"])]` est équivalent à `df[(df["day"] == "Thur") | (df["day"] == "Fri")]`, mais beaucoup plus lisible quand la liste grandit.
        :::

        ::::

        ---

        :::info 🔃 Trier les données

        Trier un DataFrame, c'est **réordonner ses lignes selon les valeurs d'une ou plusieurs colonnes**. \
        Par défaut, Pandas trie en ordre croissant (du plus petit au plus grand, ou de A à Z). On peut inverser cet ordre avec `ascending=False`.

        `sort_values()` retourne un **nouveau DataFrame** trié. Le *DataFrame* original n'est pas modifié.

        ```python
        df_trie1 = df.sort_values(by="total_bill")              # → ordre croissant par défaut
        df_trie2 = df.sort_values(by="tip", ascending=False)    # → ordre décroissant
        df_trie3 = df.sort_values(by=["day", "total_bill"])     # → tri sur 2 colonnes
        ```

        :::

        ---

        ::::info 🔗 Chaîner les opérations

        On peut **chaîner** un filtre et un tri en une seule instruction. Les deux versions ci-dessous produisent exactement le même résultat :

        ```python
        # Version détaillée : plus facile à lire et à déboguer
        df_version1 = df[df["day"].isin(["Sun", "Sat"])]
        df_version1 = df_version1.sort_values(by="total_bill")

        # Version compacte ("Method Chaining") : une seule ligne
        df_version2 = df[df["day"].isin(["Sun", "Sat"])].sort_values(by="total_bill")
        ```

        Les deux contiennent les repas de fin de semaine, triés par montant de facture.

        :::note 💡 Performance : filtrer avant de trier
        Plus tôt on réduit la taille du jeu de données, plus les opérations suivantes sont rapides. Commence toujours par **filtrer** avant de **trier**.
        :::

        ::::

        ---

        :::warning 🐛 Astuce de débogage : View as DataFrame

        Quand tu **combines** plusieurs opérations, il peut être difficile d'identifier la source d'une erreur. Le débogage est souvent plus simple en **décomposant** les instructions sur des lignes distinctes : cela permet de vérifier l'état du *DataFrame* après **chaque transformation**.

        En mode débogage, PyCharm offre une fonctionnalité très pratique : le lien **"View as DataFrame"**, visible dans le panneau des variables. Il permet d'**inspecter visuellement** le contenu d'un *DataFrame* sous forme de tableau interactif, directement depuis le débogueur.

        <Row>
          <Column size={6}>
            <div style={{ textAlign: "center" }}>
              <img
                src={require('/img/cours/r11-debogage-viewasdataframe-image1.png').default}
                alt="Lien View as DataFrame dans le débogueur PyCharm"
                style={{ maxWidth: '100%', height: 'auto' }}
              />
              <p>👆 Clique sur <strong>"View as DataFrame"</strong> dans le panneau des variables pour inspecter un <em>DataFrame</em> en cours d'exécution.</p>
            </div>
          </Column>
          <Column size={6}>
            <div style={{ textAlign: "center" }}>
              <img
                src={require('/img/cours/r11-debogage-viewasdataframe-image2.png').default}
                alt="Vue tabulaire du DataFrame dans PyCharm"
                style={{ maxWidth: '100%', height: 'auto' }}
              />
              <p>📊 Le contenu du <em>DataFrame</em> s'affiche dans une vue tabulaire claire, avec toutes ses lignes et colonnes.</p>
            </div>
          </Column>
        </Row>
        :::



        ---

        ::::note 📋 Aide-mémoire — Filtrer et trier un DataFrame

        <AideMemoirePandasFiltrerTrier />

        ::::

        <Feedback feedbackId="Rencontre #11 - Filtrer / Trier" />
    </TabItem>

    <TabItem value="pandasselectionner" label="Sélectionner">

        :::info 🎯 Qu’est-ce que sélectionner ?

        Sélectionner, c’est **extraire une partie d’un DataFrame** : certaines colonnes, certaines lignes, ou une cellule précise. C’est différent de filtrer : filtrer choisit des lignes selon une condition, sélectionner choisit quelles colonnes ou positions on veut voir.

        Selon ce qu’on sélectionne, Pandas retourne l’un de ces trois types :

        - **DataFrame** : un tableau à deux dimensions, comme `df` lui-même
        - **Series** : une seule colonne ou une seule ligne, c’est-à-dire une liste de valeurs avec un index
        - **valeur scalaire** : une seule valeur, comme un `int` ou un `float`

        Le type retourné dépend aussi des crochets utilisés :

        | Ce qu’on sélectionne | Syntaxe | Type retourné |
        |---|---|---|
        | Plusieurs colonnes | <C>df[[<S>"col1"</S>, <S>"col2"</S>]]</C> | DataFrame |
        | Une seule colonne | <C>df[<S>"col"</S>]</C> | Series |
        | Une seule colonne (format tableau) | <C>df[[<S>"col"</S>]]</C> | DataFrame |
        | Plusieurs lignes (par position) | <C>df.iloc[<N>2</N>:<N>5</N>]</C> | DataFrame |
        | Une seule ligne (par position) | <C>df.iloc[<N>3</N>]</C> | Series |
        | Une cellule (par position) | <C>df.iat[<N>3</N>, <N>0</N>]</C> | valeur scalaire |
        | Une cellule (par étiquette) | <C>df.at[<N>3</N>, <S>"tip"</S>]</C> | valeur scalaire |

        :::

        ---

        ::::info 👆 Sélectionner des colonnes

        Pour extraire une ou plusieurs colonnes d'un DataFrame, on utilise les crochets `[]`. Le type retourné dépend du nombre de crochets utilisés.

        **Plusieurs colonnes → DataFrame** (doubles crochets)

        ```python
        df_apercu = df[["total_bill", "tip", "day"]]
        print(df_apercu.head(3))
        ```
        Résultat :
        ```
           total_bill   tip  day
        0       16.99  1.01  Sun
        1       10.34  1.66  Sun
        2       21.01  3.50  Sun
        ```

        **Une seule colonne → Series** (crochets simples)

        ```python
        serie_tip = df["tip"]
        print(serie_tip.head(3))
        ```
        Résultat :
        ```
        0    1.01
        1    1.66
        2    3.50
        Name: tip, dtype: float64
        ```

        **Une seule colonne → DataFrame** (doubles crochets)

        ```python
        df_tip = df[["tip"]]
        print(df_tip.head(3))
        ```
        Résultat :
        ```
            tip
        0  1.01
        1  1.66
        2  3.50
        ```

        :::warning ⚠️ Crochets simples vs doubles : ça change tout
        `df["tip"]` retourne une **Series** (une dimension). \
        `df[["tip"]]` retourne un **DataFrame** (deux dimensions). \
        En cas de doute, utilise `print(type(...))` pour vérifier.

        ```python
        print(type(df["tip"]))     # <class ‘pandas.core.series.Series’>
        print(type(df[["tip"]]))   # <class ‘pandas.core.frame.DataFrame’>
        ```
        :::

        ::::

        ---

        ::::info 👆 Sélectionner des lignes

        `iloc` sélectionne des lignes par **position numérique**, exactement comme le slicing de liste.

        ```python
        # Plusieurs lignes → DataFrame
        df_debut = df.iloc[0:3]
        print(df_debut)
        ```
        Résultat :
        ```
           total_bill   tip     sex smoker  day    time  size
        0       16.99  1.01  Female     No  Sun  Dinner     2
        1       10.34  1.66    Male     No  Sun  Dinner     3
        2       21.01  3.50    Male     No  Sun  Dinner     3
        ```

        On peut aussi sélectionner des lignes spécifiques par leur position :

        ```python
        df.iloc[[0, 5, 10]]     # → DataFrame — lignes 0, 5 et 10 précisément
        df.iloc[[3]]            # → DataFrame — une seule ligne (doubles crochets)
        df.iloc[3]              # → Series    — une seule ligne (crochets simples)
        ```

        ::::

        ---

        :::info 🔬 Sélectionner une cellule

        Pour accéder à une seule valeur, `df.iat[]` (par position) et `df.at[]` (par étiquette) sont les méthodes recommandées.

        ```python
        # Par position : df.iat[numéro_ligne, numéro_colonne]
        valeur = df.iat[0, 1]              # ligne 0, colonne 1 → tip = 1.01

        # Par étiquette : df.at[index, nom_colonne]
        valeur = df.at[0, "tip"]           # ligne d’index 0, colonne "tip" → 1.01
        ```

        :::

        ---

        :::info 🏷️ Sélectionner par étiquette avec loc

        `loc` sélectionne par **étiquette de ligne**. Dans un DataFrame normal les étiquettes sont numériques (0, 1, 2...), donc `loc` et `iloc` donnent le même résultat. `loc` devient indispensable quand les lignes ont des étiquettes textuelles, comme avec `df.describe()` :

        ```python
        desc = df.describe()
        print(desc)
        ```
        Résultat :
        ```
               total_bill         tip        size
        count  244.000000  244.000000  244.000000
        mean    19.785943    2.998279    2.569672
        std      8.902412    1.383638    0.951100
        min      3.070000    1.000000    1.000000
        25%     13.347500    2.000000    2.000000
        50%     17.795000    2.900000    2.000000
        75%     24.127500    3.562500    3.000000
        max     50.810000   10.000000    6.000000
        ```

        Les lignes portent des étiquettes textuelles (`mean`, `min`, `max`...). On utilise `loc` pour extraire une valeur précise :

        ```python
        # Extraire la moyenne de "tip"
        mean_tip = desc.loc["mean", "tip"]        # → 2.998279

        # Extraire le maximum de "total_bill"
        max_bill = desc.loc["max", "total_bill"]  # → 50.81
        ```

        Syntaxe : `df.loc[étiquette_ligne, nom_colonne]`

        :::

        ---

        ::::note 📋 Aide-mémoire — Sélectionner dans un DataFrame

        <AideMemoirePandasSelectionner />

        ::::

        <Feedback feedbackId="Rencontre #11 - Sélectionner" />
    </TabItem>

    <TabItem value="pandasparcourir" label="Parcourir">

        ::::info 🔁 Qu’est-ce que parcourir ?

        Parcourir un DataFrame, c’est **passer sur chaque ligne ou chaque valeur une par une** pour effectuer une opération personnalisée. C’est utile quand on veut faire un traitement pour chaque élément, appliquer une logique conditionnelle complexe, ou encore construire une nouvelle liste à partir des données.

        :::note 💡 Filtrer et trier avant de parcourir
        Si tu n’as besoin que d’une partie des données, **filtre et trie ton DataFrame avant** de le parcourir. Parcourir moins de lignes, c’est un code plus rapide et plus lisible.
        :::

        ::::

        ---

        :::info 🎮 Données utilisées dans cet onglet

        Les exemples qui suivent utilisent le DataFrame `df_consoles`, qui regroupe des données sur les consoles de jeux vidéo les plus vendues. Chaque ligne représente une console, avec son année de sortie, ses ventes estimées (en millions d’unités) et son jeu le plus vendu.

        ```python
        import pandas as pd

        df_consoles = pd.DataFrame({
            "Console": ["PlayStation 5", "Xbox Series X", "Nintendo Switch 2"],
            "Année_Sortie": [2020, 2020, 2025],
            "Unités_Vendues_Millions": [92.0, 35.0, 19.6],
        })
        ```

        :::

        ---

        ::::info 🔁 Parcourir ligne par ligne avec iterrows()

        `iterrows()` permet de parcourir les lignes d’un DataFrame avec une boucle `for`. À chaque itération, on reçoit l’**index** de la ligne et son **contenu** sous forme de Series.
        Cette Series a pour index les **noms des colonnes** du DataFrame. On peut donc accéder à une valeur par nom, exactement comme dans un dictionnaire.

        **Exemple simple avec une seule colonne :**

        ```python
        for index, row in df_consoles.iterrows():
            print(row["Console"])
        ```

        *l'instruction `row["Console"]` retourne la valeur de la colonne `Console` pour la ligne courante.*


        Résultat :
        ```
        PlayStation 5
        Xbox Series X
        Nintendo Switch 2
        ```

        **Exemple avec plusieurs colonnes :**

        ```python
        for index, row in df_consoles.iterrows():
            print(f"{row["Console"]} ({row["Année_Sortie"]}) : {row["Unités_Vendues_Millions"]}M unités vendues")
        ```

        Résultat :
        ```
        PlayStation 5 (2020) : 92.0M unités vendues
        Xbox Series X (2020) : 35.0M unités vendues
        Nintendo Switch 2 (2025) : 19.6M unités vendues
        ```

        :::note 💡 Les noms `index` et `row` sont des conventions
        Tu peux les remplacer par n’importe quel nom de variable. `for i, ligne in df.iterrows()` fonctionne exactement pareil.
        :::

        :::warning ⚠️ `iterrows()` est lent sur de grands jeux de données
        Pour limiter l'impact, **filtre ton DataFrame avant** de le parcourir : moins de lignes à traiter, c'est un code plus rapide. \
        Si ton but est de **créer de nouvelles colonnes** ou **modifier des colonnes existantes**, `iterrows()` n'est pas la bonne approche. \
        Les transformations vectorielles de Pandas sont nettement plus performantes et sont vues dans l'onglet [**Transformer**](/cours/rencontre11?onglet=pandastransformer).
        :::

        ::::

        ---

        :::info 🔢 Parcourir les valeurs d’une colonne

        Pour parcourir les valeurs d’une seule colonne (une Series), on itère directement dessus comme sur une liste :

        ```python
        for console in df_consoles["Console"]:
            print(console)
        ```

        Résultat :
        ```
        PlayStation 5
        Xbox Series X
        Nintendo Switch 2
        ```

        :::

        ---

        :::info 🔄 Convertir une Series en liste Python

        Une Series Pandas n’est pas une liste Python ordinaire. Certaines fonctions Python n’acceptent que des listes, pas des Series. Pour convertir, on utilise `.tolist()` :

        ```python
        liste_consoles = df_consoles["Console"].tolist()
        print(liste_consoles) # → [‘PlayStation 5’, ‘Xbox Series X’, ‘Nintendo Switch 2’]
        ```

        Certaines fonctions Python s’attendent à recevoir une liste et produisent une erreur si on leur passe une Series. `.tolist()` règle ce problème.

        :::

        ---

        :::note 📋 Aide-mémoire — Parcourir un DataFrame

        <AideMemoirePandasParcourir />

        :::

        <Feedback feedbackId="Rencontre #11 - Parcourir" />
    </TabItem>

    <TabItem value="pandasnettoyer" label="Nettoyer">

        :::tip 💡 Pourquoi nettoyer ses données ?

        Le principe du *Garbage In, Garbage Out* s'applique directement en analyse de données : si les données de base sont erronées, tous les calculs qui en découlent le seront aussi.

        On nettoie pour trois raisons principales : éviter les **erreurs de calcul** causées par les valeurs manquantes (`NaN`), assurer la **précision** en éliminant les doublons ou les valeurs aberrantes, et **faciliter la visualisation** en évitant que des points parasites faussent l'échelle d'un graphique.

        :::

        ---

        :::info 🎮 Données utilisées dans cet onglet

        Les exemples qui suivent utilisent le DataFrame `df_joueurs`, qui contient des données volontairement imparfaites : valeurs manquantes et doublons inclus.

        ```python
        import pandas as pd

        df_joueurs = pd.DataFrame({
            "Pseudo":  ["Sakura99", "DarkLink", "Sakura99", "NovaStar", "GhostX"],
            "Score":   [4200, None, 4200, 3100, None],
            "Niveau":  ["12", None, "12", None, "5"],
            "Région":  ["QC", "ON", "QC", None, "BC"]
        })
        ```

        Résultat :
        ```
             Pseudo   Score Niveau Région
        0  Sakura99  4200.0     12     QC
        1  DarkLink     NaN   None     ON
        2  Sakura99  4200.0     12     QC
        3  NovaStar  3100.0   None   None
        4    GhostX     NaN      5     BC
        ```

        On remarque que la ligne 2 est un doublon exact de la ligne 0, que Score et Niveau ont des `NaN`, que Région est manquante pour NovaStar, et que la colonne `Niveau` a été importée comme texte (ce qu'on corrigera plus loin).

        :::

        ---

        :::info 🔍 Détecter les valeurs manquantes

        Avant de nettoyer, il est utile de savoir quelles colonnes contiennent des `NaN` et combien. `.isnull().sum()` retourne le nombre de valeurs manquantes par colonne :

        ```python
        print(df_joueurs.isnull().sum())
        ```

        Résultat :
        ```
        Pseudo    0
        Score     2
        Niveau    2
        Région    1
        dtype: int64
        ```

        On voit immédiatement que `Score` et `Niveau` ont chacun 2 valeurs manquantes, et que `Région` en a 1. `Pseudo` est complet. C'est ce qui guide le choix de nettoyage : quelles colonnes cibler et quelle approche utiliser.

        :::

        ---

        ::::info 🗑️ Supprimer les lignes avec des valeurs manquantes

        `dropna()` retourne un **nouveau DataFrame** sans modifier l'original.

        **Supprimer toutes les lignes qui contiennent au moins un NaN :**

        ```python
        df_propre = df_joueurs.dropna()
        print(df_propre)
        ```

        Résultat :
        ```
             Pseudo   Score Niveau Région
        0  Sakura99  4200.0     12     QC
        2  Sakura99  4200.0     12     QC
        ```

        Seules les lignes sans aucun `NaN` sont conservées. C'est une approche radicale : on perd beaucoup de données.

        **Supprimer uniquement si une colonne critique est manquante :**

        ```python
        df_propre = df_joueurs.dropna(subset=["Score"])
        print(df_propre)
        ```

        Résultat :
        ```
             Pseudo   Score Niveau Région
        0  Sakura99  4200.0     12     QC
        2  Sakura99  4200.0     12     QC
        3  NovaStar  3100.0   None   None
        ```

        Cette forme est plus prudente : on ne supprime que les lignes où `Score` est vide, et on conserve les autres même si elles ont d'autres `NaN`.

        :::note 💡 Retourne un nouveau DataFrame
        `dropna()` ne modifie pas `df_joueurs`. Il faut assigner le résultat à une nouvelle variable (ou à `df_joueurs` pour écraser l'original).
        :::

        :::note 🎯 Nettoie seulement les colonnes dont tu as besoin
        Le réflexe naturel est d'appeler `dropna()` sans argument pour tout nettoyer d'un coup. \
        Le problème : on perd souvent des lignes à cause de `NaN` dans des colonnes qu'on n'utilise même pas.

        **Identifie d'abord les colonnes nécessaires à ta requête.** \
        Par exemple, si ton objectif est d'afficher les 3 joueurs avec les meilleurs scores de la région QC :

        ```python
        df_joueurs[df_joueurs["Région"] == "QC"][["Pseudo", "Score"]].sort_values("Score", ascending=False).head(3)
        ```

        Cette requête utilise `Région` (filtrer), `Score` (trier) et `Pseudo` (afficher). La colonne `Niveau` n'intervient pas.

        ```python
        df_propre = df_joueurs.dropna()                              # ❌ perd aussi les lignes où Niveau est NaN
        df_propre = df_joueurs.dropna(subset=["Région", "Score"])    # ✅ cible uniquement les colonnes importantes
        ```
        :::

        ::::

        ---

        ::::info 🔁 Supprimer les doublons

        `drop_duplicates()` élimine les lignes qui sont des copies exactes d'autres lignes. C'est fréquent lors de la fusion de plusieurs fichiers.

        ```python
        df_propre = df_joueurs.drop_duplicates()
        print(df_propre)
        ```

        Résultat :
        ```
             Pseudo   Score Niveau Région
        0  Sakura99  4200.0     12     QC
        1  DarkLink     NaN   None     ON
        3  NovaStar  3100.0   None   None
        4    GhostX     NaN      5     BC
        ```

        La ligne 2 (doublon exact de Sakura99) a été supprimée. Pandas conserve la première occurrence et retire les suivantes.

        :::note 💡 Retourne un nouveau DataFrame
        Comme `dropna()`, `drop_duplicates()` ne modifie pas `df_joueurs`. Il faut assigner le résultat à une nouvelle variable (ou à `df_joueurs` pour écraser l'original).
        :::

        ::::

        ---

        ::::info 🩹 Remplacer les valeurs manquantes

        Au lieu de supprimer une ligne, on peut remplacer un `NaN` par une valeur de remplacement. Le choix de cette valeur doit avoir du sens dans le contexte.

        Contrairement à `dropna()` et `drop_duplicates()`, `fillna()` est ici utilisé pour **modifier le contenu d'une colonne directement** : on applique une transformation sur la colonne et on réassigne le résultat dans `df_joueurs`. C'est le même principe que les transformations vues dans l'onglet [**Transformer**](/cours/rencontre11?onglet=pandastransformer).

        **Remplacer par une valeur fixe (colonne texte) :**

        ```python
        df_joueurs["Région"] = df_joueurs["Région"].fillna("Inconnue")
        print(df_joueurs["Région"])
        ```

        Résultat :
        ```
        0          QC
        1          ON
        2          QC
        3    Inconnue
        4          BC
        ```

        **Remplacer par la moyenne de la colonne (colonne numérique) :**

        ```python
        moyenne_score = df_joueurs["Score"].mean()
        df_joueurs["Score"] = df_joueurs["Score"].fillna(moyenne_score)
        print(df_joueurs["Score"])
        ```

        Résultat :
        ```
        0    4200.0
        1    3766.666...
        2    4200.0
        3    3100.0
        4    3766.666...
        ```

        Les `NaN` ont été remplacés par la moyenne des scores existants : (4200 + 4200 + 3100) / 3 ≈ 3766. \
        Note que le doublon Sakura99 (ligne 2) est inclus dans ce calcul.
        
        Si tu avais d'abord supprimé les doublons, la moyenne serait différente : (4200 + 3100) / 2 = 3650. \
        **L'ordre des opérations de nettoyage influence les résultats.**

        :::note ⚠️ `.mean()` s'applique aux colonnes numériques seulement
        Pour une colonne texte, utilise une valeur fixe comme `"Inconnu"` ou la valeur la plus fréquente (`df["col"].mode()[0]`).
        :::

        ::::

        ---

        ::::info 🔧 Convertir le type d'une colonne avec `astype()`

        Pandas importe parfois des nombres sous forme de texte à cause d'un caractère inattendu dans le fichier CSV. \
        C'est le cas de la colonne `Niveau` dans `df_joueurs` : elle a été définie avec des chaînes de caractères comme `"12"` plutôt que des entiers.

        ```python
        print(df_joueurs["Niveau"].dtype)  # → object (texte)
        ```

        Avant de pouvoir faire des calculs sur `Niveau` (moyenne, comparaisons numériques, etc.), il faut d'abord gérer les `NaN`, puis convertir le type :

        ```python
        # Étape 1 : remplacer les NaN par 0 (ou une autre valeur appropriée)
        df_joueurs["Niveau"] = df_joueurs["Niveau"].fillna(0)

        # Étape 2 : convertir en nombre
        df_joueurs["Niveau"] = df_joueurs["Niveau"].astype(int)
        print(df_joueurs["Niveau"].dtype)  # → int64
        ```

        :::warning ⚠️ La conversion échoue si des valeurs non convertibles sont présentes
        Si la colonne contient des valeurs comme `"N/A"` ou `"–"`, `astype()` produira une erreur. Il faut les nettoyer avec `fillna()` ou `replace()` avant de convertir.
        :::

        ::::

        ---

        :::note 📋 Aide-mémoire — Nettoyer un DataFrame

        <AideMemoirePandasNettoyer />

        :::

        <Feedback feedbackId="Rencontre #11 - Nettoyer" />
    </TabItem>

    <TabItem value="pandastransformer" label="Transformer">

        :::tip 🔄 Qu'est-ce qu'une transformation ?

        Transformer un DataFrame, c'est **créer ou modifier une colonne** à partir des données existantes, sans toucher aux lignes. \
        On calcule de nouvelles valeurs, on convertit des types ou on remplace des codes par des étiquettes lisibles.

        La forme est toujours la même : on assigne une expression à une colonne du DataFrame. \
        Si la colonne n'existe pas encore, elle est créée. Si elle existe déjà, son contenu est remplacé :

        ```python
        df["nouvelle_colonne"] = expression   # crée une nouvelle colonne
        df["colonne_existante"] = expression  # modifie une colonne existante
        ```

        Contrairement au filtrage et au tri (qui retournent un nouveau DataFrame), les transformations **modifient `df` en place** en réassignant directement la colonne dans le DataFrame.

        :::

        ---

        :::info 📂 Données utilisées dans cet onglet

        Les exemples qui suivent utilisent le fichier `tips_dataset.csv` chargé dans un DataFrame `df` :

        ```python
        import pandas as pd

        df = pd.read_csv("tips_dataset.csv")
        ```

        :::

        ---

        ::::info 🧮 Créer une colonne par calcul numérique

        On crée une nouvelle colonne en lui assignant le résultat d'un calcul sur des colonnes existantes :

        ```python
        df["tip_percentage"] = (df["tip"] / df["total_bill"]) * 100
        ```

        Pour vérifier le résultat, on affiche les colonnes source et la colonne calculée ensemble :

        ```python
        print(df[["total_bill", "tip", "tip_percentage"]].head(3))
        ```

        Résultat :
        ```
           total_bill   tip  tip_percentage
        0       16.99  1.01        5.944673
        1       10.34  1.66       16.054159
        2       21.01  3.50       16.658258
        ```

        On peut vérifier mentalement : 1.01 / 16.99 × 100 ≈ 5.94 ✓. La colonne `tip_percentage` est maintenant disponible dans `df` comme n'importe quelle autre colonne.

        :::note 💡 Vectorisation
        Pandas applique le calcul sur toutes les lignes en une seule instruction, sans avoir besoin d'une boucle `for`. \
        C'est ce qu'on appelle une **transformation vectorielle** : c'est rapide et concis.
        :::

        ::::

        ---

        ::::info ✅ Créer une colonne par comparaison (type bool)

        On peut créer une colonne dont les valeurs sont `True` ou `False` en appliquant une condition sur une colonne existante. \
        Ces colonnes booléennes sont particulièrement utiles pour filtrer ensuite.

        **Comparaison simple :**

        ```python
        df["is_dinner"] = df["time"] == "Dinner"
        ```

        **Comparaison numérique :**

        ```python
        df["is_generous"] = df["tip"] > 5
        ```

        **Comparaison avec une liste de valeurs (`.isin()`) :**

        ```python
        df["is_weekend"] = df["day"].isin(["Sun", "Sat"])
        ```

        Pour visualiser le résultat, on peut afficher les premières lignes avec les colonnes concernées :

        ```python
        print(df[["time", "tip", "day", "is_dinner", "is_generous", "is_weekend"]].head())
        ```

        Résultat :
        ```
              time   tip   day  is_dinner  is_generous  is_weekend
        0   Dinner  1.01   Sun       True        False        True
        1   Dinner  1.66   Sun       True        False        True
        2   Dinner  3.50   Sun       True        False        True
        3   Dinner  3.31   Sun       True        False        True
        4   Dinner  3.61   Sun       True        False        True
        ```

        Résumé des colonnes créées :

        | Colonne | Type | Description |
        |---|---|---|
        | `is_dinner` | `bool` | Vrai si le repas est un souper |
        | `is_generous` | `bool` | Vrai si le pourboire dépasse 5 $ |
        | `is_weekend` | `bool` | Vrai si le jour est samedi ou dimanche |

        :::note 💡 `.isin()` vs `==`
        `==` compare à une seule valeur. `.isin([...])` compare à plusieurs valeurs à la fois et est l'équivalent d'un `or` sur plusieurs `==`.
        :::

        ::::

        ---

        :::note 🔗 Les colonnes créées s'utilisent comme toutes les autres
        Une fois créées, `tip_percentage`, `is_generous`, `is_weekend` et `is_dinner` sont disponibles dans `df` comme n'importe quelle colonne existante. Tu peux les utiliser pour filtrer, trier, sélectionner ou visualiser, exactement comme tu le ferais avec `tip` ou `day`.
        :::

        ---

        ::::info 🗺️ Mapper une colonne à l'aide d'un dictionnaire

        La méthode `map()` permet de **remplacer chaque valeur d'une colonne** par la valeur correspondante dans un dictionnaire. \
        C'est utile pour convertir des codes en noms lisibles, des abréviations en texte complet, etc.

        ```python
        df_cryptos = pd.DataFrame({
            "symbole": ["BTC", "ETH", "SOL", "ADA", "XRP", "DOT"],
            "valeur_courante": [94406.0, 3101.57, 137.55, 0.4854, 2.22, 2.80]
        })

        dict_symboles_noms = {
            "BTC": "Bitcoin",
            "ETH": "Ethereum",
            "SOL": "Solana",
            "ADA": "Cardano",
            "XRP": "Ripple",
            "DOT": "Polkadot"
        }

        df_cryptos["nom"] = df_cryptos["symbole"].map(dict_symboles_noms)
        print(df_cryptos.head())
        ```

        Résultat :
        ```
          symbole  valeur_courante       nom
        0     BTC       94406.0000   Bitcoin
        1     ETH        3101.5700  Ethereum
        2     SOL         137.5500    Solana
        3     ADA           0.4854   Cardano
        4     XRP           2.2200    Ripple
        ```

        `map()` regarde chaque valeur de `symbole`, cherche la clé correspondante dans le dictionnaire, et retourne une nouvelle colonne avec les noms complets.

        :::warning ⚠️ Valeur absente du dictionnaire
        Si une valeur de la colonne n'est pas présente comme clé dans le dictionnaire, Pandas la remplace silencieusement par `NaN`. Assure-toi que ton dictionnaire couvre toutes les valeurs possibles, ou vérifie après coup avec `df["col"].isnull().sum()`.
        :::

        ::::

        ---

        :::note 📋 Aide-mémoire — Transformer un DataFrame

        <AideMemoirePandasTransformer />

        :::

        <Feedback feedbackId="Rencontre #11 - Transformer" />
    </TabItem>

    <TabItem value="pandasgrouper" label="Statistiques">

        :::tip 📊 Qu’est-ce qu’une statistique en Pandas ?

        Cet onglet couvre deux niveaux d’analyse : les **statistiques sur une colonne** (moyenne, somme, valeurs uniques) et les **statistiques par groupe** avec `groupby()`. L’idée est toujours la même : résumer les données pour en dégager des tendances.

        :::

        ---

        :::info 📂 Données utilisées dans cet onglet

        Les exemples qui suivent utilisent le fichier `tips_dataset.csv` chargé dans un DataFrame `df` :

        ```python
        import pandas as pd

        df = pd.read_csv("tips_dataset.csv")
        ```

        :::

        ---

        ::::info 🧮 Statistiques sur une colonne

        On peut appeler des méthodes statistiques directement sur une colonne (une Series). Voici les essentielles à connaître :

        ```python
        print(df["tip"].mean())    # → 2.998...  (moyenne)
        print(df["tip"].sum())     # → 731.58    (somme)
        print(df["tip"].min())     # → 1.0       (minimum)
        print(df["tip"].max())     # → 10.0      (maximum)
        print(df["tip"].median())  # → 2.9       (médiane)
        print(df["tip"].count())   # → 244       (nombre de valeurs non-nulles)
        ```

        :::note 📐 Méthodes plus avancées
        L’écart-type (`std()`) et la variance (`var()`) mesurent la dispersion des données autour de la moyenne. Utiles en statistiques, mais moins prioritaires pour débuter.
        :::

        ::::

        ---

        ::::info 🔎 Explorer les valeurs d’une colonne catégorielle

        Pour une colonne textuelle, plutôt que des moyennes, on veut savoir quelles valeurs existent et combien de fois elles apparaissent.

        **Lister les valeurs distinctes et leur fréquence :**

        ```python
        print(df["day"].value_counts())
        ```

        Résultat :
        ```
        day
        Sat     87
        Sun     76
        Thur    62
        Fri     19
        Name: count, dtype: int64
        ```

        `value_counts()` retourne les valeurs triées du plus fréquent au moins fréquent. C’est souvent la première chose à faire sur une colonne textuelle.

        **Lister les valeurs uniques et en compter le nombre :**

        ```python
        print(df["day"].unique())    # → [‘Sun’ ‘Sat’ ‘Thur’ ‘Fri’]
        print(df["day"].nunique())   # → 4
        ```

        On constate qu’il n’y a que 4 jours dans le jeu de données : lundi, mardi et mercredi sont absents.

        :::note 💡 `nunique()` vs `unique()`
        `nunique()` ne compte pas les `NaN`, alors que `unique()` les inclut dans la liste.
        :::

        ::::

        ---

        ::::info 📊 Regrouper et calculer avec `groupby()`

        `groupby()` permet de **diviser le DataFrame en groupes** selon les valeurs d’une colonne, puis d’**appliquer une fonction statistique** sur chaque groupe.

        Pandas suit trois étapes : il **sépare** les lignes en groupes, **applique** une fonction sur chacun, puis **combine** les résultats en une Series ou un DataFrame.

        **Exemple — pourboire moyen par jour :**

        ```python
        tip_par_jour = df.groupby("day")["tip"].mean()
        print(tip_par_jour)
        ```

        Résultat :
        ```
        day
        Fri     2.734737
        Sat     2.993103
        Sun     3.255132
        Thur    2.771452
        Name: tip, dtype: float64
        ```

        La colonne de regroupement (`day`) devient l’**index** du résultat, et les valeurs sont les statistiques calculées par groupe.

        **Plusieurs statistiques à la fois avec `agg()` :**

        ```python
        df_stats = df.groupby("day")["tip"].agg(["mean", "sum", "count"])
        print(df_stats)
        ```

        Résultat :
        ```
                  mean     sum  count
        day
        Fri   2.734737   51.96     19
        Sat   2.993103  260.40     87
        Sun   3.255132  247.39     76
        Thur  2.771452  171.83     62
        ```

        :::note 🔍 Exemple avancé — regrouper sur plusieurs colonnes
        On peut regrouper sur plusieurs colonnes à la fois. Le résultat contient une ligne par combinaison de valeurs :

        ```python
        df.groupby(["sex", "day"])[["tip", "total_bill"]].agg(["mean", "count"])
        ```

        Cela produit un DataFrame à colonnes hiérarchiques (une paire `mean`/`count` par colonne analysée). \
        Utile pour des analyses croisées, mais la lecture du résultat est plus complexe.
        :::

        ::::

        ---

        :::info 📋 Tables de pivot

        `pivot_table()` ressemble à `groupby()`, mais organise les résultats en **tableau à deux dimensions** pour faciliter la comparaison visuelle. \
        On choisit ce qui va sur les lignes, ce qui va sur les colonnes, et quelle valeur on calcule dans les cellules.

        ```python
        df_pivot = df.pivot_table(
            values="tip",          # valeur à calculer
            index="size",          # regroupement sur les lignes
            columns="day",         # regroupement sur les colonnes
            aggfunc="mean"         # fonction statistique
        )
        print(df_pivot)
        ```

        Résultat :
        ```
        day          Fri       Sat       Sun      Thur
        size
        1        1.000000  2.000000  1.500000       NaN
        2        2.250000  2.592857  3.070588  2.428571
        3        3.000000  3.622222  3.050000  2.250000
        4        1.500000  3.500000  4.030303  3.683333
        5             NaN  2.750000  4.566667  2.600000
        6             NaN  5.000000       NaN  5.300000
        ```

        Chaque cellule contient le pourboire moyen pour une taille de groupe et un jour donnés. Les `NaN` indiquent qu’aucune donnée n’existe pour cette combinaison.

        :::

        ---

        :::note 📋 Aide-mémoire — Statistiques

        <AideMemoirePandasStatistiques />

        :::

        <Feedback feedbackId="Rencontre #11 - Statistiques" />
    </TabItem>

    <TabItem value="pandasexporter" label="Exporter">

        :::tip 💾 Pourquoi exporter ses données ?

        Une fois les données nettoyées, transformées ou analysées, on veut souvent les sauvegarder pour les réutiliser ailleurs : les partager avec un collègue, les ouvrir dans Excel, les intégrer dans une autre application. Pandas permet d'exporter un DataFrame en quelques lignes.

        :::

        ---

        :::info 📂 Données utilisées dans cet onglet

        Les exemples qui suivent utilisent le fichier `tips_dataset.csv` chargé et transformé dans un DataFrame `df` :

        ```python
        import pandas as pd

        df = pd.read_csv("tips_dataset.csv")
        df["tip_percentage"] = (df["tip"] / df["total_bill"]) * 100
        ```

        :::

        ---

        :::info 🔁 Préparer les données avant d'exporter

        On prépare d'abord le DataFrame à exporter en enchaînant les manipulations souhaitées. Ici, on filtre les repas du soir avec un pourboire supérieur à 15 % et on trie par pourcentage décroissant :

        ```python
        df_export = df[(df["time"] == "Dinner") & (df["tip_percentage"] > 15)].sort_values("tip_percentage", ascending=False)
        ```

        `df_export` est maintenant prêt à être sauvegardé dans le format de ton choix.

        :::

        ---

        ::::info 📄 Exporter en CSV

        Le format CSV est le plus universel : lisible par Excel, Python, R, et la plupart des outils d'analyse.

        ```python
        df_export.to_csv("soupers_genereux.csv", index=False)
        ```

        Le fichier `soupers_genereux.csv` est créé dans le même dossier que ton script.

        :::note 💡 `index=False`
        Par défaut, Pandas ajoute une colonne d'index (0, 1, 2...) au début du fichier. `index=False` évite cette colonne superflue dans la plupart des cas.
        :::

        ::::

        ---

        ::::info 📊 Exporter en Excel (.xlsx)

        Pour exporter vers Excel, il faut avoir la bibliothèque `openpyxl` installée :

        ```python
        pip install openpyxl
        ```

        Ensuite, l'export se fait en une ligne :

        ```python
        df_export.to_excel("soupers_genereux.xlsx", index=False)
        ```

        Le fichier `soupers_genereux.xlsx` est créé dans le même dossier que ton script.

        :::note 💡 `index=False`
        Par défaut, Pandas ajoute une colonne d'index (0, 1, 2...) au début du fichier. `index=False` évite cette colonne superflue dans la plupart des cas.
        :::

        ::::

        ---

        :::note 📋 Aide-mémoire — Exporter un DataFrame

        <AideMemoirePandasExporter />

        :::

        <Feedback feedbackId="Rencontre #11 - Exporter" />
    </TabItem>

    <TabItem value="pandasvisualiser" label="Visualiser">

        :::tip 📊 Pandas + Matplotlib

        Pandas s'intègre directement avec Matplotlib pour générer des graphiques à partir d'un DataFrame. \
        On importe les deux bibliothèques, on charge les données, et on trace en quelques lignes seulement.

        N'hésitez pas à consulter la [documentation officielle de Matplotlib](https://matplotlib.org/stable/contents.html) pour plus de détails et d'exemples.

        :::

        ---

        :::info 📂 Données utilisées dans cet onglet

        Les exemples qui suivent utilisent le fichier `tips_dataset.csv` chargé dans un DataFrame `df`, avec une colonne calculée `tip_percentage` :

        ```python showLineNumbers
        import pandas as pd
        import matplotlib.pyplot as plt

        df = pd.read_csv("tips_dataset.csv")
        df["tip_percentage"] = (df["tip"] / df["total_bill"]) * 100
        ```

        :::

        ---

        :::info 📊 Histogramme : distribution d'une colonne numérique

        Un histogramme montre comment les valeurs d'une colonne sont réparties. L'axe X est divisé en intervalles de valeurs, et l'axe Y indique combien de données tombent dans chaque intervalle. Le paramètre `bins` détermine le nombre d'intervalles. Pandas calcule automatiquement la largeur de chaque intervalle en divisant l'écart entre la valeur minimale et maximale par le nombre de bins :

        > largeur d'un intervalle = (max − min) / bins

        Par exemple, si `tip_percentage` va de 3 % à 71 % et qu'on demande `bins=20`, chaque intervalle aura une largeur d'environ (71 − 3) / 20 ≈ 3,4 %.

        <Row>
            <Column>
                ```python showLineNumbers
                df["tip_percentage"].hist(
                    bins=20  # diviser en 20 intervalles
                )
                plt.title("Distribution du pourcentage de pourboire")
                plt.xlabel("Pourcentage de pourboire (%)")
                plt.ylabel("Fréquence")
                plt.grid(axis='both', linestyle='--', linewidth=0.5)
                plt.show()
                ```
            </Column>
            <Column>
                <img src={require('/img/cours/r11_histogramme_pourcentage_pourboire.png').default} style={{ maxWidth: '100%', height: 'auto' }} />
            </Column>
        </Row>

        :::

        ---

        :::info 📦 Boîte à moustaches : distribution par groupe

        Une boîte à moustaches (*boxplot*) montre la distribution d'une variable numérique pour chaque groupe. Voici comment lire chaque élément du graphique :

        Comment interpréter une boîte à moustaches :
        - La **ligne du milieu (orange)** représente la **médiane** : 50 % des valeurs sont en dessous, 50 % au-dessus.
        - Le **bas de la boîte** représente le **1er quartile (Q1)** : 25 % des valeurs sont en dessous.
        - Le **haut de la boîte** représente le **3e quartile (Q3)** : 75 % des valeurs sont en dessous.
        - La **hauteur de la boîte** est l'**intervalle interquartile (IQR)** = Q3 − Q1 : il contient le 50 % central des données.
        - Les **moustaches (les traits)** s'étendent jusqu'à 1,5 × IQR au-delà de Q1 et Q3.
        - Les **cercles (•)** sont les **valeurs aberrantes** (*outliers*) : valeurs qui dépassent les moustaches.

        <Row>
            <Column>
                ```python showLineNumbers
                df.boxplot(
                    column="tip_percentage",  # axe des y
                    by="day"                  # regroupement (axe des x)
                )
                plt.title("Pourcentage de pourboire par jour")
                plt.suptitle("")  # retire le titre autogénéré
                plt.xlabel("Jour")
                plt.ylabel("Pourcentage de pourboire (%)")
                plt.grid(axis='both', linestyle='--', linewidth=0.5)
                plt.show()
                ```
            </Column>
            <Column>
                <img src={require('/img/cours/r11_boxplot_pourcentage_pourboire_par_jour.png').default} style={{ maxWidth: '100%', height: 'auto' }} />
            </Column>
        </Row>

        :::

        ---

        :::info 🔵 Nuage de points : relation entre deux colonnes

        Un nuage de points (*scatter plot*) montre la relation entre deux variables numériques. On peut ajouter une troisième variable en faisant varier la taille des points.

        <Row>
            <Column>
                ```python showLineNumbers
                plt.scatter(
                    x=df["total_bill"],  # axe des x
                    y=df["tip"],         # axe des y
                    s=df["size"] * 20,   # taille des points (×20 pour la visibilité)
                    alpha=0.5            # opacité
                )
                plt.xlabel("Montant total de la facture ($)")
                plt.ylabel("Pourboire ($)")
                plt.title("Pourboire selon la facture\n(taille ∝ nombre de convives)")
                plt.grid(axis='both', linestyle='--', linewidth=0.5)
                plt.show()
                ```
            </Column>
            <Column>
                <img src={require('/img/cours/r11_size.png').default} style={{ maxWidth: '100%', height: 'auto' }} />
            </Column>
        </Row>

        :::

        ---

        :::info 📈 Courbes groupées : comparer des tendances

        En combinant `pivot_table()` et `.plot()`, on peut tracer plusieurs courbes sur un même graphique pour comparer des tendances selon une variable catégorielle.

        <Row>
            <Column>
                ```python showLineNumbers
                df["is_weekend"] = df["day"].isin(["Sun", "Sat"])

                df_pivot = df.pivot_table(
                    values="tip",         # axe des y
                    index="size",         # axe des x
                    columns="is_weekend", # une courbe par groupe
                    aggfunc="mean"
                )

                df_pivot.plot(marker="o")
                plt.xlabel("Taille du groupe")
                plt.ylabel("Pourboire moyen ($)")
                plt.title("Pourboire moyen par taille de groupe\n(semaine vs fin de semaine)")
                plt.legend(
                    title="Période",
                    labels=["Semaine", "Fin de semaine"]
                )
                plt.grid(axis='both', linestyle='--', linewidth=0.5)
                plt.show()
                ```
            </Column>
            <Column>
                <img src={require('/img/cours/r11_pivot.png').default} style={{ maxWidth: '100%', height: 'auto' }} />
            </Column>
        </Row>

        :::

        ---

        :::note 📋 Aide-mémoire — Visualiser avec Pandas et Matplotlib

        <AideMemoirePandasVisualiser />

        :::

        <Feedback feedbackId="Rencontre #11 - Visualiser" />
    </TabItem>

    <TabItem value="Activités" label="Activités">

        <RappelFonctionnementClasseInverse/>
        <Tabs>
            <TabItem value="1" label="🏠 Activités à faire à la maison">
                ## 🧠 Auto-validation des connaissances

                [**Ce formulaire**](https://forms.gle/PVFuF6aH8kcRrbaYA) sert à vérifier votre compréhension des éléments les plus importants de la rencontre R11. Ne faites ce questionnaire **que lorsque vous vous sentez en très bonne maîtrise de la matière**. En classe, dès votre arrivée, vous aurez un **questionnaire très similaire**, évalué **sommativement**, à compléter sans accès à **aucune documentation**.

                ::::warning
                Ne pas remplir le formulaire diminue fortement vos chances de réussir l’évaluation sommative en début de rencontre. \
                Assurez-vous de bien comprendre toutes les notions derrière chaque question.
                ::::
            </TabItem>

	        <TabItem value="2" label=" 🏫 Activités à faire en classe">
                ## ✏️ Exercices de compréhension

                :::info Exercice - Écrire du code sans PyCharm
                📁 Créez un fichier `r11_sans_pycharm.py` pour votre réponse.

                Sur papier, sans ordinateur, écrivez un code demandant à l'utilisateur de saisir un nombre. \
                Tant que le nombre saisi est positif, le programme redemande de saisir un nouveau nombre. \
                Lorsqu'un nombre saisi est négatif, le programme affiche tous les nombres saisis ainsi que leur somme. \
                Le programme prend alors fin.

                #### Exemple d'exécution

                ```
                Saisir un nombre : 20
                Saisir un nombre : 50
                Saisir un nombre : 10
                Saisir un nombre : -5
                Nombres saisis : [20, 50, 10, -5]
                Somme : 75
                ```
                **Validez** votre réponse à l'aide de **Pycharm**.


                :::

                ---

                ## 🔨 Exercices de création

                :::info Exercice - groupby et moyenne
                📁 Créez un fichier `r11_groupby_moyenne.py` pour votre réponse.

                Téléchargez le fichier <a href={useBaseUrl('/ressources/csv/tips_dataset.csv')} download="tips_dataset.csv">tips_dataset.csv</a> et utilisez 🐼 Pandas pour :
                1. Charger les données dans un *DataFrame*.
                2. Afficher les 10 premières lignes.
                3. Calculer et afficher la moyenne du pourboire (`tip`) :
                    1. pour les clients fumeurs et non-fumeurs,
                    2. par jour de la semaine,
                    3. par temps (dîner/lunch),
                    4. par taille de groupe.

                Résultats attendus :
                <Row>
                    <Column size={3}>
                        ```
                        smoker
                        No     2.991854
                        Yes    3.008710
                        Name: tip, dtype: float64




                        ```
                    </Column>
                    <Column size={3}>
                        ```
                        day
                        Fri     2.734737
                        Sat     2.993103
                        Sun     3.255132
                        Thur    2.771452
                        Name: tip, dtype: float64


                        ```
                    </Column>
                    <Column size={3}>
                        ```
                        time
                        Dinner    3.102670
                        Lunch     2.728088
                        Name: tip, dtype: float64




                        ```
                    </Column>
                    <Column size={3}>
                        ```
                        size
                        1    1.437500
                        2    2.582308
                        3    3.393158
                        4    4.135405
                        5    4.028000
                        6    5.225000
                        Name: tip, dtype: float64
                        ```
                    </Column>
                </Row>

                - Quelle(s) variable(s) semble(nt) influencer le plus le montant du pourboire ?
                - Quelle(s) variable(s) semble(nt) n'avoir aucun impact ?


                :::

                ---

                :::info Exercice - Analyse : Modèles de véhicules
                📁 Créez un fichier `r11_modeles_vehicules.py` pour votre réponse.

                Toujours avec le fichier <a href={useBaseUrl('/ressources/csv/mpg_dataset.csv')} download="mpg_dataset.csv">mpg_dataset.csv</a>.<br />
                Dans un nouveau script, vous devez :

                1. Charger les données dans un *DataFrame*.
                2. Créer des nouveaux *DataFrames* à partir du *DataFrame* de départ vous permettant d'obtenir ces informations :
                    1. Les modèles japonais ayant une accélération de 19.4 et plus.
                    2. Les 5 voitures avec le plus de horsepower.
                    3. Le nombre de voitures japonaises avec un poid supérieur à 2500.
                3. Présenter les résultats de ces *DataFrames* exactement comme ci-dessous :

                #### Résultats attendus
                ```
                Les modèles japonais ayant une accélération de 19.4 et plus :
                    - toyota corolla 1200
                    - mazda glc deluxe
                    - datsun 210 mpg

                Les 5 voitures avec le plus de horsepower :
                    - horsepower : 230.0 - pontiac grand prix
                    - horsepower : 225.0 - buick electra 225 custom
                    - horsepower : 225.0 - pontiac catalina
                    - horsepower : 225.0 - buick estate wagon (sw)
                    - horsepower : 220.0 - chevrolet impala

                Le nombre de voitures japonaises avec un poid supérieur à 2500 : 17
                ```
                **Indices**

                - Pour l'affichage des résultats, consultez la section *"Parcourir un DataFrame et une Series"* de [cet onglet](/cours/rencontre11?onglet=pandasselectionner).
                :::

                ---

                :::info Exercice - Analyse : Prix Turing
                📁 Créez un fichier `r11_prix_turing.py` pour votre réponse.

                Téléchargez le fichier <a href={useBaseUrl('/ressources/csv/Turing.csv')} download="Turing.csv">Téléchargez le fichier Turing.csv</a>.
                - Placez-le dans le même dossier que votre script Python.
                - Il contient tous les gagnants du Prix Turing.

                Dans un nouveau script, vous devez :
                1. Charger les données dans un *DataFrame*.
                2. Créer des nouveaux *DataFrames* à partir du *DataFrame* de départ vous permettant d'obtenir ces informations :
                    1. Nombre total de lauréats.
                    2. Lauréats avec 'intelligence' dans la contribution.
                    3. Années avec 3 lauréats ou plus.
                3. Présenter les résultats de ces *DataFrames* exactement comme ci-dessous :

                <br/>
                #### Résultat attendu :
                ```
                Nombre total de lauréats : 79

                Lauréats avec 'intelligence' dans la contribution :
                    - Marvin Minsky
                    - Allen Newell
                    - Herbert A. Simon
                    - Raj Reddy

                Années avec 3 lauréats ou plus :
                    - 2002
                    - 2007
                    - 2018
                ```
                **Indices** \
                La dernière question d'analyse est la plus compliquée, voici quelques indices :
                    - **regrouper** les données par années et utilisez la fonction statistique permettant de **compter** le nombre d'éléments;
                    - **filtrer** ensuite le DataFrame pour n'obtenir que les lignes dont au moins une colonne contient une valeur de 3 ou plus ;
                    - **parcourir** le DataFrame et utilisez l'index pour l'affichage.


                :::

                ---

                :::info Exercice - Visualisation : Pourboire par personne
                📁 Créez un fichier `r11_pourboire_par_personne.py` pour votre réponse.

                Toujours avec le même jeu de données, créez une nouvelle colonne `tip_par_person` qui calcule le pourboire par personne (i.e. `tip` divisé par `size`). Ensuite, affichez les 5 premières lignes du DataFrame modifié.

                Résultat attendu :
                ```
                   total_bill   tip     sex smoker  day    time  size  tip_par_person
                0       16.99  1.01  Female     No  Sun  Dinner     2        0.505000
                1       10.34  1.66    Male     No  Sun  Dinner     3        0.553333
                2       21.01  3.50    Male     No  Sun  Dinner     3        1.166667
                3       23.68  3.31    Male     No  Sun  Dinner     2        1.655000
                4       24.59  3.61  Female     No  Sun  Dinner     4        0.902500
                ```

                Ensuite, générez ces 2 graphiques :
                1. un histogramme de cette nouvelle colonne `tip_par_person` pour visualiser la distribution des pourboires par personne en 20 intervalles égaux .
                2. un boxplot de `tip_par_person` regroupé par taille de groupe (`size`) pour voir comment le pourboire par personne varie en fonction de la taille du groupe.

                Résultats attendus :
                <Row>
                  <Column size={6}>
                    <div style={{ textAlign: "left" }}>
                      <img
                        src={require('/img/cours/r11_exercice_2a.png').default}
                        alt="Histogramme tip par person"
                        style={{ maxWidth: '100%', height: 'auto' }}
                      />
                    </div>
                  </Column>
                    <Column size={6}>
                        <div style={{ textAlign: "right" }}>
                        <img
                            src={require('/img/cours/r11_exercice_2b.png').default}
                            alt="Boxplot tip par person par size"
                            style={{ maxWidth: '100%', height: 'auto' }}
                        />
                        </div>
                    </Column>
                </Row>

                <br />

                Voyez-vous des tendances intéressantes ?


                :::

                ---

                :::info Exercice - Visualisation : Consommation d'essence
                📁 Créez un fichier `r11_consommation_essence.py` pour votre réponse.

                Téléchargez le fichier <a href={useBaseUrl('/ressources/csv/mpg_dataset.csv')} download="mpg_dataset.csv">mpg_dataset.csv</a>.<br />
                Dans un nouveau script :
                1. Chargez les données dans un *DataFrame*.
                2. Créez une nouvelle colonne `l_par_100km` en convertissant `mpg` en litres aux 100 km.
                3. Générez ces 2 graphiques :
                <Row>
                  <Column size={6}>
                    <div style={{ textAlign: "left" }}>
                      <img
                        src={require('/img/cours/r11_exercice_3a.png').default}
                        alt="Histogramme tip par person"
                        style={{ maxWidth: '100%', height: 'auto' }}
                      />
                    </div>
                  </Column>
                    <Column size={6}>
                        <div style={{ textAlign: "right" }}>
                        <img
                            src={require('/img/cours/r11_exercice_3b.png').default}
                            alt="Boxplot tip par person par size"
                            style={{ maxWidth: '100%', height: 'auto' }}
                        />
                        </div>
                    </Column>
                </Row>

                <br />

                Voyez-vous des tendances intéressantes ?


                :::

                ---


                # 🎯 Solutions des exercices

                Un solutionnaire possible pour chaque exercice est disponible en format vidéo avec des explications.
                Vous pouvez vous en servir pour **comparer votre solution** avec une solution possible jugée optimale et
                **vous débloquer après un long** moment bloqué sur un exercice (**ex. 20 minutes**) et après avoir utilisé le dégogueur pour essayer par vous-même de trouver le problème.

                :::warning
                Il est tout à fait normal que certains problèmes demandent du temps, de la réflexion, et parfois même un peu de frustration.
                C’est précisément dans **ces moments d’effort que l’apprentissage s’ancre réellement**.

                Consulter un solutionnaire **avant d’avoir tenté l’exercice par soi-même**, ou le parcourir trop rapidement, revient à **court-circuiter le processus d’apprentissage**.
                Ce n’est pas simplement contre-productif, c’est **pédagogiquement désastreux**.

                En sautant l’étape de la réflexion personnelle, on prive son cerveau de l’occasion de construire des connexions durables.
                Et à force de répéter ce réflexe, on risque de **passer à côté des compétences essentielles**, ce qui peut mener à des échecs plus tard, même si tout semble facile sur le moment. Alors oui, prenez le temps. L’erreur fait partie du jeu. C’est en cherchant, en tâtonnant, en doutant, qu’on devient réellement compétent. Le solutionnaire doit être un outil de validation, **pas un raccourci**.

                :::

                🔨 Solution des **exercices de création** :
                - [Groupe et moyenne](https://youtu.be/vhL5TKFluwo)
                - [Analyse : Modèles de véhicules](https://youtu.be/lxp0zy9nSXs)
            </TabItem>
        </Tabs>
    </TabItem>

</Tabs>