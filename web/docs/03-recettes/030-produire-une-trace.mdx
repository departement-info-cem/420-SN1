---
hide_table_of_contents: true
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

import TraceFonction from '@site/docs/_components/TraceFonction.mdx';
import TraceBoucle from '@site/docs/_components/TraceBoucle.mdx';
import TraceCondition from '@site/docs/_components/TraceCondition.mdx';

# 🕵️‍♂️ Trace d’exécution

:::info Trace

Lorsqu'on a un morceau de code, on peut souhaiter savoir dans quel ordre les instructions sont exécutées.<br />
On veut spécifier précisément :
- Quelles lignes de code s'exécutent dans quel ordre.
- Les effets de chaque ligne de code :
    - quelles variables sont créées/modifiées,
    - quelles valeurs elles contiennent,
    - ce qui est affiché (si on a des `print`).
- La portée des variables.
- Si on a des fonctions :
    - la pile d'appels (quelles fonctions sont en cours d'exécution),
    - quelle fonction est appelée,
    - quelle valeur elle retourne (si elle en retourne une).

:::

:::tip-nt
👉 Vous pouvez imprimer ce [tableau de trace d'exécution](/pdf/Traces_recto_verso.pdf) pour pratiquer vos traces à la main.
:::

<Tabs queryString="recette-activite">
<TabItem value="Exemple simple" label="Exemple simple">

<Row>
<Column>

### Code
Si on a le code suivant :

```python  showLineNumbers
a = 3 + 4
b = a * 2
print(f'salut {b}')
```

</Column>
<Column>

    ### Trace
    On aura la trace suivante :

    {/*| ligne exécutée | effet |
    |-----------------|-----------------------------------|
    | a = 3 + 4 | a: 7 |
    | b = a * 2 | a: 7, b: 14 |
    | print(f'salut \{b\}') | a: 7, b: 14 <br/> affiche "salut 14" |*/}

    | # ligne   | a     | b     | affichage |
    |-----------|-------|-------|-----------|
    | 1         | 7     |       |           |
    | 2         | 7     | 14    |           |
    | 3         | 7     | 14    | salut 14  |

</Column>
</Row>

Dans le cas d'une séquence d'instructions, on peut voir que les instructions sont exécutées dans
l'ordre de lecture du code. Simple!

</TabItem>

<TabItem value="Avec fonction" label="Avec fonction">

    <TraceFonction />

</TabItem>

<TabItem value="condition" label="Avec condition">

    <TraceCondition />

</TabItem>

<TabItem value="Avec boucle" label="Avec boucle">

    <TraceBoucle />

</TabItem>

<TabItem value="valider-une-trace" label="Valider une trace">

Afin de valider une trace :
1. Placer un point d'arrêt sur la première ligne qui s'exécute.
2. Lancer l'exécution en mode débogage.
3. Sauter de ligne en ligne pour voir si la trace est correcte.

Si on se rend à la fin de l'exécution et que toutes tes valeurs étaient bonnes,
on a bien compris ce que fait le code.

</TabItem>
</Tabs>