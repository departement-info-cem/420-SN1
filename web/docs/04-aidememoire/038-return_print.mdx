---
hide_table_of_contents: true
---

# ⚠️ `return` / `print`

Beaucoup mélangent `return` et `print`.

👉 **Règle d’or :**

- `print()` ➜ affiche à l’écran
- `return` ➜ renvoie une valeur à l’endroit où la fonction a été appelée

Ce n’est **pas la même chose**.

<Row>
<Column>

    :::info-nt

    ## 1️⃣ Ce que fait `print()`

    ```python
    def dire_bonjour():
        print("Bonjour!")

    resultat = dire_bonjour()
    print("Valeur retournée :", resultat)
    ```

    ### 🔎 Affichage obtenu :

    ```
    Bonjour!
    Valeur retournée : None
    ```

    ### ❗ Pourquoi ?

    - La fonction affiche `"Bonjour!"`
    - Mais elle **ne retourne rien**
    - Donc Python retourne automatiquement `None`

    :::

</Column>
<Column>

    :::info-nt

    ## 2️⃣ Ce que fait `return`

    ```python
    def dire_bonjour():
        return "Bonjour!"

    resultat = dire_bonjour()
    print("Valeur retournée :", resultat)
    ```

    ### 🔎 Affichage obtenu :

    ```
    Valeur retournée : Bonjour!
    ```

    ### ✅ Cette fois :

    - La fonction **renvoie** la chaîne
    - On peut l’utiliser ailleurs
    - On peut l’afficher
    - On peut la stocker
    - On peut la modifier

    :::

</Column>
</Row>

# 🎯 Différence essentielle

| `print()` | `return` |
|------------|----------|
| Affiche une valeur | Renvoie une valeur |
| Sert à voir quelque chose à l’écran | Sert à transmettre une information |
| Ne permet pas de réutiliser la valeur | Permet de réutiliser la valeur |
| Retour implicite : `None` | Stoppe la fonction et renvoie une valeur |

---

# ⚠️ Erreur fréquente

<Row>
<Column>

    :::danger-nt

    ## ❌ Mauvaise façon

    ```python
    def carre(n):
        print(n * n)

    resultat = carre(4)
    print("Le résultat est :", resultat)
    ```

    ### 🔎 Affichage :

    ```
    16
    Le résultat est : None
    ```

    👉 Le calcul a été affiché,
    mais **rien n’a été retourné**.

    :::

</Column>
<Column>

    :::tip-nt

    ## ✅ Bonne façon

    ```python
    def carre(n):
        return n * n

    resultat = carre(4)
    print("Le résultat est :", resultat)
    ```

    ### 🔎 Affichage :

    ```
    Le résultat est : 16
    ```

    :::

</Column>
</Row>

# 🧠 Question à se poser

Avant d’écrire `print` dans une fonction, il faut se demander :

- Est-ce qu'on veut seulement afficher le résultat?
- Ou est-ce qu'on veut pouvoir l’utiliser ailleurs ?

Si on veut l’utiliser ➜ `return`

---

# 🧪 Test rapide

Que va afficher ce code ?

```python
def addition(a, b):
    print(a + b)

x = addition(3, 5)
print(x)
```

👉 Réponse :

```
8
None
```

Pourquoi ?
Parce que la fonction affiche le résultat
mais ne le retourne pas.

---

# 🚀 Résumé final

- `print()` montre
- `return` transmet
- Une fonction bien conçue **retourne une valeur**
- On utilise `print()` à l’extérieur pour afficher le résultat

---

💡 En examen :
Si on vous demande **“écrire une fonction qui retourne…”**,
alors il faut utiliser `return`, pas `print`.