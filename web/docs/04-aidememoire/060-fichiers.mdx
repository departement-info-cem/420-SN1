---
hide_table_of_contents: true
---

import { S, N, K, F, B, C } from '@site/docs/_components/PyCode.mdx';

# 📁 Fichiers

## 🔹 Lire un fichier texte

```python
with open("fichier.txt", "r", encoding="utf-8") as f:
    contenu = f.read()  # Lit tout le contenu dans une seule chaîne
    print(contenu)
```

---

### Ligne par ligne (pratique pour les grands fichiers)

```python
with open("fichier.txt", "r", encoding="utf-8") as f:
    for ligne in f:
        print(ligne.strip())  # .strip() enlève le saut de ligne final
```

---

### Toutes les lignes dans une liste

```python
with open("fichier.txt", "r", encoding="utf-8") as f:
    lignes = f.readlines()
print(lignes)  # Liste contenant chaque ligne (saut de ligne inclus)
```

💡 `.readlines()` est pratique si on veut tout en mémoire, mais moins efficace pour les très gros fichiers.

---

## 🔹 Écrire dans un fichier

```python
with open("fichier.txt", "w", encoding="utf-8") as f:
    f.write("Bonjour\n")
```

⚠️ Le mode `"w"` écrase le contenu existant.

---

### Ajouter à la fin du fichier

```python
with open("fichier.txt", "a", encoding="utf-8") as f:
    f.write("Nouvelle ligne\n")
```

---

## 🔹 Lire un fichier CSV

```python
import csv

with open("donnees.csv", "r", encoding="utf-8") as f:
    lecteur = csv.reader(f)
    for ligne in lecteur:
        print(ligne)
```

---

## 🔹 Résumé des modes d'ouverture

| Mode | Action |
|------|--------|
| <C><S>"r"</S></C> | Lecture |
| <C><S>"w"</S></C> | Écriture (écrase le contenu existant) |
| <C><S>"a"</S></C> | Ajout à la fin |

---

💡 Avec <C><K>with</K> <B>open</B>(...)</C>, le fichier est automatiquement fermé.
